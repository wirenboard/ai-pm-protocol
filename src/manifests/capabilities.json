{
  "_comment": [
    "Harness-neutral vocabulary layer (plan scenario 2; arch note 'How thin the",
    "metalanguage can be'). A capability is named ONCE, neutrally; each harness's",
    "manifest resolves it to that harness's concrete primitive. The generator reads",
    "this lookup to decide HOW a capability is realized in the target adapter.",
    "",
    "Slice 1 ships only the 'claude' column populated. The 'opencode' column is",
    "present as documented intent (sourced from doc/stack-notes.md) so a later slice",
    "plugs OpenCode in by filling primitives + adding an opencode adapter manifest,",
    "WITHOUT re-carving any neutral body. The bodies in src/ are already harness-",
    "neutral; nothing here rewrites them.",
    "",
    "For Claude (the reference harness) every primitive is realized by an artifact",
    "that already lives in the Claude manifest (frontmatter blocks, settings.json),",
    "so the slice-1 substitutions are effectively identity: the generator emits the",
    "verbatim Claude artifact. The structure — not a near-identity Claude mapping —",
    "is what lets a non-identity harness slot in later.",
    "",
    "harness_reference (harness-neutral-prose slice 1): the READER-FACING side of",
    "the same single source. The shared prose (pm-* agent/command bodies, and a",
    "later slice WORKFLOW.md / workflow/*.md) names every harness-specific concept",
    "with a NEUTRAL NOUN, never a bare Claude/OpenCode primitive. This block is the",
    "ONE home that maps each neutral noun -> its Claude and OpenCode concrete. A",
    "human-readable table is GENERATED from this block (gen/generate.py --harness",
    "reference -> doc/harness-reference.md); a reader who needs the concrete consults",
    "that table; the prose stays neutral. `claude_concrete` is also the exact bare",
    "string the neutral-prose guard (tests/neutral-prose.sh) scans for OUTSIDE the",
    "allowlist. Single source — do NOT copy this mapping anywhere else."
  ],
  "harness_reference": {
    "_note": [
      "Each entry: neutral_noun (the word the prose uses), claude_concrete and",
      "opencode_concrete (the per-harness realization the reader resolves to via the",
      "generated table), and an optional `skew` flag where the two harnesses differ",
      "BEHAVIORALLY, not just in naming — the route reminder fires per-prompt on",
      "Claude but is always-on on OpenCode; the prose names the INTENT neutrally and",
      "each adapter doc states its realized timing (arch note sub-decision 2)."
    ],
    "project-entry-file": {
      "neutral_noun": "the project entry file",
      "claude_concrete": "CLAUDE.md",
      "opencode_concrete": "AGENTS.md",
      "note": "The harness-loaded project instruction file. The protocol's Pipeline block, the `## Project kind:` line, and the Docs table all live in whichever file the harness loads."
    },
    "structured-question-tool": {
      "neutral_noun": "the structured-question tool",
      "claude_concrete": "AskUserQuestion",
      "opencode_concrete": "question",
      "note": "Surface a substantive product fork as a structured choice, not free prose."
    },
    "enforcement-layer": {
      "neutral_noun": "the enforcement layer",
      "claude_concrete": "settings.json PreToolUse hook",
      "opencode_concrete": "tool.execute.before plugin (throw)",
      "note": "Blocks a role-duplicator spawn/skill-load and guards boundary/destructive ops at the harness level."
    },
    "instruction-loading-mechanism": {
      "neutral_noun": "the instruction-loading mechanism",
      "claude_concrete": "@-import (@.ai-pm/tooling/WORKFLOW.md)",
      "opencode_concrete": "instructions[] array",
      "note": "How the always-on core (WORKFLOW.md) is loaded for every session."
    },
    "change-intent-route-reminder": {
      "neutral_noun": "the change-intent route reminder",
      "claude_concrete": "UserPromptSubmit hook",
      "opencode_concrete": "always-on AGENTS.md/instructions content",
      "skew": "BEHAVIORAL: per-prompt on Claude (fires on change-intent), always-on on OpenCode (no UserPromptSubmit-equivalent). Name the intent neutrally; each adapter doc states its realized timing.",
      "note": "Inject the protocol route on change-intent prompts."
    },
    "skill-invocation-tool": {
      "neutral_noun": "the skill-invocation tool",
      "claude_concrete": "Skill",
      "opencode_concrete": "skill",
      "note": "Invoke a packaged skill/command."
    },
    "adapter-directory": {
      "neutral_noun": "the adapter directory",
      "claude_concrete": ".claude/",
      "opencode_concrete": ".opencode/",
      "note": "The harness's adapter tree (agents, commands, enforcement). Context-sensitive — the install/enforcement prose that names a concrete adapter path is the adapter layer's own concern (allowlisted in the install command bodies until the install-auto-detect slice)."
    }
  },
  "capabilities": {
    "subagent-definition": {
      "description": "How a pm-* subagent's metadata (name, description, tool grant) is declared alongside its neutral body.",
      "claude": {
        "primitive": "yaml-frontmatter-block",
        "realized_by": "claude/frontmatter/<name>.fm (verbatim per-agent block: name + description + comma-list tools)",
        "tools_form": "comma-list"
      },
      "opencode": {
        "primitive": "yaml-frontmatter-block",
        "realized_by": "opencode/frontmatter/<name>.fm (description + mode: subagent + tools OBJECT map; model/permission deferred — cross-model pins are a later slice)",
        "tools_form": "object-map",
        "source": "https://opencode.ai/docs/agents/",
        "status": "BUILT in slice 2"
      }
    },
    "command-definition": {
      "description": "How a /pm-* command body is declared. On Claude the command file is pure body, no frontmatter.",
      "claude": {
        "primitive": "bare-markdown-body",
        "realized_by": "the neutral command body, emitted verbatim (no frontmatter)"
      },
      "opencode": {
        "primitive": "markdown-body-with-optional-at-inline",
        "realized_by": ".opencode/command/<name>.md (neutral body emitted verbatim; command bodies MAY use @file inline — an asymmetry the generator does NOT over-apply to agent bodies)",
        "source": "https://opencode.ai/docs/commands/",
        "status": "BUILT in slice 2"
      }
    },
    "deny-list-enforcement": {
      "description": "Block a class-1 role-duplicator spawn/skill-load; guard boundary/destructive ops.",
      "claude": {
        "primitive": "pretooluse-hook-in-settings-json",
        "realized_by": "claude/settings.json hooks.PreToolUse (emitted verbatim)"
      },
      "opencode": {
        "primitive": "ts-plugin-throw",
        "realized_by": ".opencode/ plugin: tool.execute.before throws to deny / mutates output.args to rewrite",
        "source": "https://opencode.ai/docs/plugins/",
        "status": "doc-cited (unverified) — Spike B (#5894) gates subagent containment"
      }
    },
    "route-reminder": {
      "description": "Inject the protocol route on change-intent prompts.",
      "claude": {
        "primitive": "userpromptsubmit-hook-in-settings-json",
        "realized_by": "claude/settings.json hooks.UserPromptSubmit (emitted verbatim; per-prompt injection)"
      },
      "opencode": {
        "primitive": "always-on-instructions-content",
        "realized_by": "baked into AGENTS.md / instructions (always-on, NOT per-prompt — no UserPromptSubmit-equivalent hook)",
        "source": "https://opencode.ai/docs/plugins/",
        "status": "doc-cited (unverified)"
      }
    },
    "always-on-core-import": {
      "description": "Make the always-on core (WORKFLOW.md) load for every session.",
      "claude": {
        "primitive": "at-import-line",
        "realized_by": "the @WORKFLOW.md import in CLAUDE.md (out of slice-1 scope — adapter slice covers .claude/ only)"
      },
      "opencode": {
        "primitive": "instructions-array-entry",
        "realized_by": ".opencode/opencode.json instructions[] -> .ai-pm/tooling/WORKFLOW.md (submodule-relative path); no @-import inside an instruction file",
        "source": "https://opencode.ai/docs/rules/",
        "status": "BUILT in slice 2"
      }
    },
    "structured-question-tool": {
      "description": "Surface a substantive product fork as a structured choice, not free prose.",
      "claude": { "primitive": "AskUserQuestion" },
      "opencode": {
        "primitive": "question",
        "realized_by": "the OpenCode runtime `question` tool (no per-harness adapter file — the neutral agent bodies name the structured-question capability harness-agnostically); available to the slice-2 .opencode/ subagents",
        "source": "https://opencode.ai/docs/tools/",
        "status": "BUILT in slice 2 (runtime tool; no emitted artifact)"
      }
    },
    "skill-invocation": {
      "description": "Invoke a packaged skill/command.",
      "claude": { "primitive": "Skill" },
      "opencode": {
        "primitive": "skill",
        "realized_by": "the OpenCode runtime `skill` tool (no per-harness adapter file — the neutral bodies name skill-invocation harness-agnostically); available to the slice-2 .opencode/ subagents (e.g. pm-coder/pm-stack-researcher carry skill: true)",
        "source": "https://opencode.ai/docs/skills/",
        "status": "BUILT in slice 2 (runtime tool; no emitted artifact)"
      }
    }
  }
}
