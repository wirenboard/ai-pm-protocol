{
  "_doc": "The quality-tools registry — the one home for 'what does GREEN mean in this project'. The template ships only this SHAPE plus the example rows below; a real project REPLACES the examples with its own stack's tools and drops each tool's native config beside this file (.eslintrc, ruff.toml, semgrep.yml, …). The Builder runs every `build`-beat tool and hands back only when they pass; the Reviewer confirms the `review`-beat tools ran and reads their output. No core edit is ever needed to add or change a tool — only a row here.",
  "_row_shape": {
    "id": "short stable name",
    "checks": "one plain line: what this tool catches",
    "config": "path to its native config beside this file, or null if none",
    "run": "the command to run it (from the project root)",
    "beat": "build | review | ship — which loop beat it runs in",
    "init": "one line: how to install/enable it (the minimal init instruction)"
  },
  "tools": [
    {
      "id": "parity",
      "checks": "adapter integrity — parity across both shims, single-engine (no rule logic leaked into a shim), the Claude node-spike",
      "config": null,
      "run": "node src/adapter/parity.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "install-model",
      "checks": "OpenCode install bake — a concrete reviewer-model pin lands in the assembled frontmatter; auto/session/absent emit no model line",
      "config": null,
      "run": "node src/adapter/install-model.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "install-plugin",
      "checks": "OpenCode plugin generation — the committed deployed plugin (.opencode/plugins/ai-dev.mjs) is byte-identical to the generator's output, so source and deployed cannot drift; the adapter-path rewrite is correct per layout",
      "config": null,
      "run": "node src/adapter/install-plugin.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "install-drift",
      "checks": "assembled-artifact anti-drift — every committed generated markdown artifact (both platforms' agents + commands) is byte-identical to a fresh assembly under the tracked .ai-dev/config.json, AND every committed deployed on-demand procedure (.ai-dev/procedures/) is byte-identical to its src/agents/procedures/ source; no orphan file in a generated/deployed dir",
      "config": null,
      "run": "node src/adapter/install-drift.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "install-modules",
      "checks": "capability-module assembly — the shared assembler composes an enabled module's fragment into the role agent, omits it when off, never drops the floor body, fails safe to ON (malformed/unknown toggle ⇒ on), throws on a missing fragment for an enabled module, and rejects a root-escaping fragment pointer",
      "config": null,
      "run": "node src/adapter/install-modules.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "rigor-profile",
      "checks": "configurable rigor — the engine resolves the profile to solo by default (absent/unknown/malformed/unconfigured) and relaxes the orchestrator-content deny under lite/solo, denying only under an explicit full; the floor (tooling, boundary, truncation, merge-gate, stamp-write) never relaxes under any profile",
      "config": null,
      "run": "node src/adapter/rigor-profile.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "safeguards",
      "checks": "configurable safeguards — the engine's general evaluate-level skip honours .ai-dev/config.json `safeguards`: a toggleable ask/inject guard listed \"off\" is skipped, while a rule without `toggleable:true` (every deny + the merge-gate) is never skippable however config reads; only an exact-string \"off\" disables (unknown value / non-object / malformed ⇒ all guards on); disabledSafeguards returns the well-formed off-ids and safeguardRegistry lists exactly the seven toggleable guards",
      "config": null,
      "run": "node --test src/adapter/safeguards.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "components",
      "checks": "multi-repo components — the fail-CLOSED manifest loader/validator (engine.mjs componentRoots) collapses to the single session root on absent/malformed/wrong-shape/non-existent/overbroad/partial-honour, canonicalises declared roots through realpath (defeats symlink escapes), and returns the validated multi-root set on a valid manifest. This is the validator unit-tested in isolation; the boundary wiring (the set consulted by the read/find/write project-boundary denies, with the per-root `.ai-dev/tooling/` carve-out) is covered by parity.test.mjs.",
      "config": null,
      "run": "node src/adapter/components.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "multirepo-e2e",
      "checks": "multi-repo boundary END-TO-END through the REAL entry — a real `node shim.mjs` subprocess resolves the session root from a real on-disk git-init'd multi-repo fixture (the resolveRoot seam parity.test.mjs bypasses) and enforces the widened boundary: declared-sibling allow, non-declared deny, the D6 seam-contract-as-transport allow, the per-root `.ai-dev/tooling/` carve-out deny, and the no-manifest single-root regression tripwire. Validates the MECHANISM the orchestrator's `[persona]` coordination relies on, not the coordination itself. OpenCode asymmetry recorded honestly (no subprocess entry; plugin-entry not source-loadable) — its real decide() driven against the same fixture.",
      "config": null,
      "run": "node src/adapter/multirepo-e2e.test.mjs",
      "beat": "build",
      "init": "Node >= 18 + git, no install"
    },
    {
      "id": "merge-gate",
      "checks": "the unstamped-review merge-gate parses commands correctly — the pushed/merged ref outranks .git/HEAD (cross-branch push reads the right stamp), any branch prefix strips (feature/, fix/, bare), non-shell heredoc bodies are data (no verb false positives; shell-heredoc and malformed bodies stay matched), the accepted stamp labels are exactly the documented two, and an UNRESOLVABLE topic asks, never passes",
      "config": null,
      "run": "node src/adapter/merge-gate.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "f4-floor",
      "checks": "F4 mechanical floor — a blind bulk-stage (git add -A/./--all/*) denies while a named-path / .gitignore / -p add allows; a git commit on a main/master checkout of a configured project WITH commit history and no satisfied trunk stamp denies, while the carve-outs allow (a reviewed main-named change, the day-zero bootstrap of an unconfigured or fresh-init repo, yolo). DENY-class on both platforms.",
      "config": null,
      "run": "node src/adapter/f4-floor.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "forge-map",
      "checks": "forge-map.json shape — the persona-read forge-issue verb map parses as JSON, carries the three supported forges (github/gitlab/gitea), and each forge declares all five backlog operations (create/list/view/close/comment) with a runnable command string. Guards the reference (no engine loads it) from rotting silently; does not assert flag accuracy.",
      "config": null,
      "run": "node src/adapter/forge-map.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "ssh-content-edit",
      "checks": "the remote-edit DENY floor (ssh-content-edit) distinguishes a remote WRITE from a redirect-using remote READ: a stream redirect (2>/dev/null, > /dev/null, 2>&1) ALLOWS while a `>`/`>>` redirect to a real file, sed -i, tee, and vi/vim/nano still DENY (floor unchanged); fail-closed — a stream-lookalike (/dev/null2, /dev/null/../etc/passwd) is treated as a write and denies; the predicate stays inert on a non-ssh local redirect.",
      "config": null,
      "run": "node src/adapter/ssh-content-edit.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "error-handling",
      "checks": "adapter fail-direction under malformed input — a broken deny-rules.json makes the Claude shim subprocess fail OPEN (exit 0, allow, log to stderr, no crash); a bad regex in a config pattern makes the inject predicates return allow (no nudge), never throw; a valid config + pattern still injects (the happy path is unbroken)",
      "config": null,
      "run": "node src/adapter/error-handling.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "install-commands",
      "checks": "command assembly — the /dev-setup command is generated into each platform's commands dir from the one source, identical in substance across platforms (invariant 6 one-home), with the platform's own front-matter",
      "config": null,
      "run": "node src/adapter/install-commands.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "opencode-inject",
      "checks": "the OpenCode inject realisation — the plugin registers only the tool.execute.before deny hook and NOT the crashing chat.message hook (opencode 1.17.8 EventV2 crash), so inject-class rules fall back to persona on OpenCode; the canonical enforcement-realisation pattern reviewer.md points at",
      "config": null,
      "run": "node src/adapter/opencode-inject.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "registry-coverage",
      "checks": "inverse orphan-guard — every committed src/**/*.test.mjs is referenced by a tools.json row, so a test file cannot be added then silently never run (the gap the 2026-06-20 audit found)",
      "config": null,
      "run": "node src/quality/registry-coverage.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "install",
      "checks": "the unified installer — installs into a temp target inside the root, vendors the adapter self-sufficiently (the vendored installer can re-run), wires both platforms (hooks/agents/commands/plugin), stamps .ai-dev/VERSION + writes the UPGRADING marker on a version change, lays the migration notes + the inactive-platform breadcrumb, replaces stale ai-dev hook groups, resolves the platform (flag/config/error), never clobbers a real doc or file, and a second run is byte-identical (idempotent)",
      "config": null,
      "run": "node src/adapter/install.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "model-router-test",
      "checks": "the per-seat cross-endpoint model router (src/adapter/model-router.mjs) through its REAL http path against STUB upstreams (no network, no real keys): a request routes to the backend its model matches and reaches no other; the per-backend auth header is swapped to the right scheme AND value (x-api-key vs Authorization: Bearer, the backend key not the client front-key, the incoming client auth stripped); a passthrough route (auth:\"passthrough\") forwards the client's auth VERBATIM with no swap while a key-swap route still strips+swaps; the request body + a chunked/streamed response pass through intact; the error paths fail cleanly — an oversize body ⇒ 413, an unreachable upstream ⇒ 502, a mid-stream upstream socket-drop after headers does not crash the router and the partial reaches the client; an unknown model, a missing model, an unparseable body, and a route whose key env is unset all FAIL CLOSED (4xx/5xx, never a silent default backend); and the opt-in routing log leaks no key, no body, no header (only model -> host)",
      "config": null,
      "run": "node src/adapter/model-router.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "router-launch-test",
      "checks": "the model-router launcher's PURE decision functions (src/adapter/router-launch.mjs), no network / no `claude` exec: a routes config's { provider: <id> } entry resolves base_url + auth from the built-in catalog (src/adapter/model-providers.json) with inline match/base_url/auth overrides and provider-pattern fan-out; an unknown provider and an unsupported passthrough both throw; distinct-endpoint counting drives the launch mode — all-Anthropic seats run claude DIRECT (proxy off by default), cross-endpoint seats start the router; the key-env presence check fails closed (an unset backend key for a route in play is surfaced BEFORE launch, passthrough routes skipped); seatModels keeps concrete role pins and drops auto/session; buildChildEnv sets ANTHROPIC_BASE_URL and UNSETS CLAUDE_CODE_SUBAGENT_MODEL; and the REAL shipped catalog pins the four verified providers (anthropic passthrough + x-api-key, deepseek x-api-key, glm z.ai Coding-Plan Bearer + GLM-*, openrouter Bearer)",
      "config": null,
      "run": "node src/adapter/router-launch.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "version-skew",
      "checks": "installed VERSION stamp matches vendored tooling VERSION — a skew means npx updated the stamp without re-vendoring the tooling; no-op on a source checkout where .ai-dev/VERSION is absent",
      "config": null,
      "run": "node src/quality/version-skew.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "transient-hygiene",
      "checks": "ADVISORY (never red, always exit 0) — surfaces stale loop transients: a .ai-dev/plans|reviews|audit file whose topic matches no open local branch (already shipped, the ship-beat delete skipped). Silent when clean; emits a GitHub Actions ::warning:: per stale file under CI so the nudge reaches the Operator on the PR. Fail-safe no-op on no-git/absent-dir",
      "config": null,
      "run": "node src/quality/transient-hygiene.mjs",
      "beat": "build",
      "init": "Node >= 18 + git, no install"
    },
    {
      "id": "transient-hygiene-test",
      "checks": "the transient-hygiene advisory's own logic against a real git fixture — a live-branch topic is kept, an orphaned plan / _review stamp / audit run flags, a main-only checkout flags all, and absent-dir/non-git fail-safe to no finding; the script stays exit-0 (advisory) in every case",
      "config": null,
      "run": "node src/quality/transient-hygiene.test.mjs",
      "beat": "build",
      "init": "Node >= 18 + git, no install"
    },
    {
      "id": "run-quality",
      "checks": "the whole-set quality runner itself — filters by beat, fails closed on a malformed registry, no-op-safe when empty/absent",
      "config": null,
      "run": "node src/quality/run.test.mjs",
      "beat": "build",
      "init": "Node >= 18, no install"
    },
    {
      "id": "neutral-prose",
      "checks": "the core (PROTOCOL.md, agents, docs/architecture.md) names no bare platform primitive",
      "config": null,
      "run": "node src/quality/neutral-prose.test.mjs",
      "beat": "review",
      "init": "Node >= 18, no install"
    },
    {
      "id": "eslint",
      "checks": "JS lint (the engine, shims, install scripts, the runner, the tests) — unused bindings, accidental globals, loose equality, var. Standard recommended ruleset, made STRICTER not looser; code is brought to the standard, never the standard to the code. Plus a SOFT file-size surface (`max-lines` WARN at ~800; never an error — surfaces an oversized file to `decompose`, stays green since the run carries no `--max-warnings 0`).",
      "config": "eslint.config.mjs",
      "run": "npx eslint .",
      "beat": "build",
      "init": "npm install"
    },
    {
      "id": "markdownlint",
      "checks": "Markdown format for the docs — list/heading spacing, table column style, fenced-code language, emphasis-as-heading. Standard ruleset; the only relaxations (MD013 line-length, MD024 siblings-only, MD041 partials) are recorded Operator decisions in the config.",
      "config": ".markdownlint-cli2.jsonc",
      "run": "npx markdownlint-cli2",
      "beat": "build",
      "init": "npm install"
    },
    {
      "id": "semgrep",
      "checks": "security static analysis (SAST) over the JS — command injection, path traversal, unsafe patterns. Standard published rulesets (p/javascript + p/security-audit), no custom loosening.",
      "config": null,
      "run": "semgrep --config p/javascript --config p/security-audit --error --metrics off --exclude node_modules src/",
      "beat": "review",
      "init": "pip install semgrep (Python); the rulesets fetch from the public Semgrep registry on first run"
    }
  ]
}
