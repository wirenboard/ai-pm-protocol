# Plan compliance review — orchestrator-anti-corner-cutting, PIECES 1+2

Scope: sub-branch `feature/opencode-harness-support--anti-corner-cutting-p12` implements ONLY plan pieces 1 (graceful subagent-failure path, persona-primary) and 2 (pre-code/pre-ship artifact-gate, plugin). Piece 3 (spike-gated chat-hook reminder) is out of scope and correctly absent — NOT flagged.

Note on numbering: the PLAN renumbers vs the ARCH NOTE. Plan piece 1 = failure-path (arch piece 1); plan piece 2 = artifact-gate (arch's "Piece 3" section). Verified against the PLAN numbering as instructed.

## Plan compliance

**Piece 1 — graceful subagent-failure (scenarios 1–4, failure-inventory):**
- ✓ Canonical cross-harness home present — `workflow/enforcement.md` `### Graceful subagent-failure`: retry N=2 → STOP+report → never self-substitute verdict/code/stamp/merge; "failed-agent artifact = a MISSING artifact, never a pass"; refusal treated identically. Harness-neutral prose (neutral-prose.sh green).
- ✓ Reinforced in the OpenCode persona — `src/manifests/opencode/harness_local/body/ai-pm.body.md` `## When a subagent fails` (lines 37–45), regenerated byte-identically into `.opencode/agent/ai-pm.md` (lines 105–113; body↔generated diff exit 0).
- ✓ Persona-strengthening rider present (`## Conditional steps are DEFAULT-ON`, body 47–53 / agent 115–121): in-doubt→user-facing→pm-product-advocate; after-coding ALWAYS pm-architect; "which agent did I call at each step" self-check before ship.
- ✓ Denied-merge = stop-not-loop clause present (body line 45 / agent 113).
- ✓ Test `oc-failure-path-persona` (tests/opencode.sh) — presence-grep of all four clauses incl. N=2 and refusal-sibling. Passes.
- ✓ Test `persona-conditional-default-on` (tests/opencode.sh) — rider. Passes.

**Piece 2 (h) pre-ship merge gate (scenarios 5, 7; interaction scenarios 2, 3):**
- ✓ Implemented in `src/manifests/opencode/plugin/ai-pm-enforcement.js.tmpl` guard (h) (lines 591–614 + helpers `resolveMergeTopic` 379–395, `reviewStampSatisfied` 406–423). Orchestrator `git merge`/`git push` of a feature branch with missing/unstamped `.ai-pm/reviews/<topic>_review.md` → THROW. Generated copy byte-identical to template (only the `__WB_DENY_ROLES__` substitution differs — pre-existing generator behavior).
- ✓ Partial-stamp (Verdict present, `## Code review: NOT YET RUN`) = unsatisfied → deny (interaction scenario 3).
- ✓ Stamped merge proceeds (no false denial); `.ai-pm` bookkeeping + non-feature/pure-git ops still allowed; fail-open on unresolvable topic / non-orchestrator actor.
- ✓ (a)–(g) behavior byte-identical: the unit diff is add-only (no deletions in the s15 region); all 39 prior unit assertions still pass.
- ✓ ESM single-export intact: only `AiPmEnforcement` exported; (h) helpers are module-scope non-exported. `oc-plugin-esm-loadable` passes.
- ✓ Tests (tests/oc-plugin-unit.js): `oc-gate-merge-deny-unstamped` (sc.5), `-allow-stamped` (sc.7), `-partial-stamp-denied` (interaction sc.3), `-bookkeeping-still-allowed` (sc.7), `-merge-nonorch-failopen`. All pass.
- ✓ Test `oc-gate-deny-is-stop-not-loop` (tests/opencode.sh, persona prose) covers interaction scenario 2. Passes.

**Piece 2 (i) pre-code write gate — DOWNGRADE to persona-only (plan-licensed):**
- ✓ (a) Honestly documented: plugin (i) note (template lines 128–146) states no deny arm is added "by design, not omission"; a pinning unit test `oc-gate-precode-no-plan-allowed-by-plugin` asserts the plugin ALLOWS a pm-coder content write with no plan, so the downgrade cannot silently regress.
- ✓ (b) Justified: no reliable write→topic signal in `tool.execute.before`; the only proxy ("deny when zero plans exist") would both be a fragile guess and contradict the s15 (g) invariant that a subagent authors content legitimately (asserted by the existing s15 unit tests). Matches the arch note's "PARTIAL / fail-open on ambiguity" reasoning.
- ✓ (c) Persona carries "A plan precedes code — always" (body line 27 / agent 95), with (h) as the deny-side floor (a planless, reviewless feature cannot ship).
- This is an acceptable plan-honored deviation, explicitly licensed by the plan ("if a clean structural signal isn't available … DOWNGRADE the other to a persona rule + a comment note rather than a fragile guess"). NOT a silent scope drop.

**Docs:**
- ✓ `doc/stack-notes.md` (11) known-limitation note (session-insert long-session crash) added.
- ✓ (10) to-verify tag NOT flipped (correct — spike-gated, piece 3 out of scope).
- ✓ `doc/architecture.md` decision record correctly DEFERRED to post-coding pm-architect (plan line 92) — expected-pending, not a gap.
- ✓ Claude golden byte-identical: no `.claude/` or `.golden/` files touched; generator-deterministic + single-source-diff-clean green; only `.opencode/` regenerated.

## Definition of Done
- [x] All plan scenarios (pieces 1+2) implemented and tested
- [x] Interaction scenarios have concurrent-state tests (sc.2 → oc-gate-deny-is-stop-not-loop; sc.3 → oc-gate-partial-stamp-denied; sc.1 belongs to piece 3, out of scope)
- [x] Stack expectations respected; stack-spec tests pass (throw-to-deny + (4b) containment cited in test comments; ESM single-export preserved)
- [n/a] Product Contract — backend/protocol-internal change, NOT user-facing (state file confirms "NOT user-facing — no advocate gate"). No Product Contract touched.
- [x] Pipeline green — oc-plugin-unit.js 46/46, opencode.sh 36/36, hooks.sh 79/79, generator.sh 4/4, neutral-prose.sh 5/5, targeted-reading.sh 7/7, ultra-absent.sh 2/2
- [x] State file updated (`.ai-pm/state/current.md`)
- [n/a] Product Impact Report — no contract touched
- [x] Docs updates landed (stack-notes (11), enforcement.md, persona body); architecture.md deferred to post-coding pm-architect per plan
- [x] Expected artifacts exist (plan, this review; no contract — not user-facing)
- [n/a] Product-readiness gate — not user-facing (every scenario subject is the orchestrator/plugin/subagent, a system actor)
- [n/a] Validation gate — software-kind project
- [x] Failure-inventory negative-space tests present — piece 1 scenarios 1–4 (failure/refusal/crash) are failure-inventory; covered by oc-failure-path-persona (piece 1 is persona text, not unit-testable per the arch note's test surface) + the merge-deny consequence backstop tests

**DoD: pass**

## Blocking
(none)

## Notes (product)
1. `workflow/enforcement.md` `### Artifact gate` describes the **pre-code write gate** as "best-effort **denied**" in the present tense, but the only realization (the OpenCode plugin) **downgraded** this arm to persona-only — no deny arm ships. The plan permitted the downgrade and the plugin (i) comment + the pinning unit test document it thoroughly, so this is not a functional defect; but the canonical cross-harness rule home now claims a deny that no shipped realization performs. Why it matters: a future reader (or a Claude-side port) may believe a pre-code deny exists and skip building it. Consider rephrasing that bullet to "best-effort denied **where a clean structural signal exists; otherwise persona-only**" so the doc matches the shipped downgrade. Non-blocking — the post-coding pm-architect docs reconciliation (already deferred) is the natural place to align this.

## Verdict
approve

<!-- The trail below is the ONE review section the orchestrator owns, not pm-plan-checker. -->
## Code review findings
Pass-2 `code-review` (pieces 1+2; base `feature/opencode-harness-support`). Two finders: plugin merge-gate logic + tests/prose. Tests CLEAN (no false-pass, no weakened s15 cases, genuine 46 fixtures); the real defects were in the (h) merge-gate + the new enforcement prose. 9 findings, all confirmed against the files:

Plugin merge-gate:
1. (false-allow, HIGH) `reviewStampSatisfied` rejected only the literal `NOT YET RUN` → an EMPTY `## Code review:` heading (delete just the NYR words) passed = trivial self-stamp bypass.
2. (false-deny, HIGH) gate hardcoded `## Code review:` → a documentation-kind project (whose load-bearing pre-ship stamp is `## Validation:`) was falsely denied a validated merge.
3. (false-allow) a FAILED/negative stamp passed (same root as #1: "not NYR" ≠ "passed").
4-5. (false-allow, MED) `git push origin HEAD:main` and `git checkout main && git merge <non-feature-branch>` resolve topic=null → fail-open → unstamped work can ship.
6. (false-deny, MED) refspec `git push origin feature/foo:main` mis-extracted topic as `foo:main` → wrong review path → denied a stamped feature.
7. (fail-open) worktree (`.git` is a file) / detached HEAD → topic=null → fail-open bypass.
Prose:
8. (HIGH) `### Artifact gate` described the pre-code write gate as "denied" though that arm was downgraded to persona-only (no plugin deny) — a future Claude-port would build a phantom deny.
9. (HIGH) over-claim "self-stamp-then-merge is denied" — the gate checks stamp PRESENCE, not PROVENANCE; a hand-written fake stamp (the exact nula failure) passes.

## Code review: FIXED — `60151f7`
All 6 MUST-FIX closed + finding 7 documented (not fixed, by design). (1)/(3) `reviewStampSatisfied` now = line exists AND content non-empty (trimmed) AND not `NOT YET RUN` (no rigid `— passed` token, so real `FIXED — <sha>` / `<date> — passed` stamps still pass); (2) accepts EITHER `## Code review:` OR `## Validation:` via a shared `stampOK(label)`; (6) topic terminator class extended to stop at `: ~ ^ @` (refspec syntax) so `feature/foo:main` → `foo`; (8) prose retitled "pre-code write (PERSONA-ONLY, no plugin deny)"; (9) merge-gate claim scoped honestly (missing/NYR/empty stamp denied; FABRICATED stamp stays persona — gate checks presence not provenance; does not forbid the orchestrator's legitimate Pass-2 trail ownership); path typo `docs/`→`doc/` fixed. (4)/(5)/(7) residual fail-open ship-paths documented in the plugin (h) comment + enforcement.md as known best-effort limitations (the gate is a floor; the real ship is the PM merging on GitHub) — the "target=main → deny" arm deliberately NOT added (string-only parse can't distinguish ship from legit sync/bookkeeping → false-deny risk). Re-verified independently: oc-plugin-unit 54/54 (+8), opencode 36/36, generator 4/4 (Claude byte-identical), neutral-prose 5/5, hooks 79/79, targeted-reading 7/7, ultra-absent 2/2.

## Verdict (Pass 2): approve
Note: the (h) merge gate is honestly a FLOOR — it catches the lazy un-stamped/NYR self-merge (and the doc-kind case), not a determined hand-fabricated stamp; that residual is persona-scoped + reinforced by piece 1's failure-path discipline and (later) piece 3's per-prompt reminder.
