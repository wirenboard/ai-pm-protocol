# product-md-front-door — plan compliance review

Branch `feature/product-md-front-door` vs `main`. This is a change to the protocol's
own template/agents/commands (markdown, no app code). Per `doc/architecture.md`,
there is no automated harness for prose procedures — `tests/hooks.sh` is the only
test artifact (meta-infrastructure exception). Verification is by inspection of the
markdown against the plan; "missing automated tests" is NOT a blocking finding here.

## Plan compliance

- ✓ **Scenario 1** — authored `docs/product.md` front door (funnel `## Зачем это нужно` / `## Что умеет сегодня` / `## Документы` / `## Функции`) exists as template `doc/_templates/product.md.tmpl`; never generated. Template states it is authored, owned by `pm-architect`, and written without any generated-map signature line. Includes the "что пока НЕ умеет" boundary example. Never regenerated by auditor/pm-plan (confirmed per-writer below).
- ✓ **Scenario 2** — generated map relocated to `docs/product-map.md`, rebuilt wholesale. The Product map generation procedure (`pm-bootstrap.md`) writes only `product-map.md`; both `/pm-plan` sites and bootstrap (all 3 modes) write only the map; auditor reads/compares only the map.
- ✓ **Scenario 3 (format fixes 2-6)** — all present in the procedure AND the worked example:
  - clickable `[<name>](../.ai-pm/contracts/<name>.md)` link, explicitly "do not print a raw backtick path";
  - `Guarantees:` line from the contract's `## User value` (explicitly "not the technical first Must work item");
  - status legend line at top of map (`live` / `deprecated`);
  - "Do **not** print a generator-mechanics header" guard;
  - `↑ та же работа` for a multi-contract feature — worked example shows `scene-engine` rendered fully under `dimmer-control` (alphabetically first) and `↑ та же работа` under `scene-recall`.
- ✓ **Scenario 4 (Migration Variant A)** — detection keys on BOTH guards: signature line present in `product.md` AND `product-map.md` absent. Steps: `git mv` (content preserved) + scaffold authored `product.md` from template without signature. Idempotent via two independent guards (signature gone after migration; `product-map.md` now present). Greenfield-before-first-feature authored funnel with no signature is left untouched (no clobber).
  - **Critical (signature coupling):** the frozen detection string `> Source of truth = contracts. One contract, many features. Generated, not hand-filled.` was NOT tidied to match the new header. It carries an explicit "frozen historical artifact — do NOT tidy/normalize" note. The new generator no longer emits it (fix #5), which is what makes the two states distinguishable. Coupling risk handled correctly.
- ✓ **Scenario 5 (bootstrap product Q&A → pm-architect authoring)** — greenfield gains 2 product questions (why/for whom, deliberately-out-of-scope-now); answers passed to `pm-architect` in the spawn prompt. pm-architect owns the authored funnel via a new Section A sub-section, with a 4th update trigger (coverage-changed, scoped to authored sections only). Sole-writer and no-signature invariants stamped. Legacy shallow and full modes both wired identically (ask 2 questions → spawn pm-architect → derive coverage from contracts/architecture → PM one-pass validates).
- ✓ **Scenario 6** — same migration entry covered in scenario 4; idempotency and no-clobber confirmed.

## Arch-notes invariants (one-sentence "writes only X, never Y" per writer)

- ✓ Product map generation procedure + both `/pm-plan` sites: "writes only `docs/product-map.md`; never creates or edits the authored `docs/product.md`."
- ✓ pm-architect: "sole writer of the authored `docs/product.md`; never writes the generated `docs/product-map.md`."
- ✓ pm-auditor: "only re-derives and compares `docs/product-map.md` ... never touches the authored `docs/product.md`" + "structure-only check ... never validate the prose."

## Four risk sites

- ✓ Both `/pm-plan` regeneration sites retargeted: plan-creation (`pm-plan.md:244` "Update the product map") AND post-approve (`:252` "Regenerate `docs/product-map.md`"). Easy-to-miss `:244` is present.
- ✓ `doc/_templates/CLAUDE.md.tmpl` no longer calls `product.md` "auto-generated" — now "Product front door — authored PM funnel ... Not generated", and a new `docs/product-map.md` row advertising the funnel-vs-map split.
- ✓ Greenfield-before-first-feature: auditor "map missing → note" explicitly skips on a legitimately feature-less project (no contracts and no plans yet); Variant A's signature guard (not mere absence) leaves the authored funnel untouched.
- ✓ `doc/architecture.md` updated for the authored/generated split (committed `12e6756`); the template-repo "deliberate exception" note updated to cover both files.

## pm-auditor split

- ✓ Generated-map block: all six sub-checks carried over verbatim, retargeted to `product-map.md` (map missing / contract not rendered / plan in neither table nor Infrastructure / Built-changed-by out of sync / dead Review link or missing Done / contract under non-matching component).
- ✓ Authored-file block: structure-only existence + four-funnel-header check, with explicit "Never validate the prose content" guard.

## Definition of Done

- [x] All plan scenarios implemented (verified by inspection — no automated harness for prose, per the meta-infra exception)
- [x] Interaction scenarios honored — separation is structural: each writer carries a "writes only X, never Y" invariant; migration rename + scaffold guarded; `/pm-plan` writes the map not the front door; pm-architect vs pm-auditor write distinct files. (No concurrent-state automated test applies — prose procedures, meta-infra exception.)
- [x] Stack expectations respected — none touched (plan: no external stack component); `tests/hooks.sh` unchanged and green
- [x] Product Contract — no Product Contract touched (this repo's "product" is the protocol itself; template-repo exception per `doc/architecture.md`). Backend/meta-only change.
- [x] Pipeline green — `bash tests/hooks.sh` → 65/65, hooks.sh byte-identical to main
- [x] State file — no spurious `.ai-pm/state/current.md` present in the branch tree (the coder-added one was removed by the orchestrator; confirmed absent). This repo does not maintain a live state file.
- [x] Product Impact Report — N/A (no contract touched)
- [x] Docs updates landed — README (v2.3 migration section + capability descriptions retargeted), `CLAUDE.md.tmpl` (Docs table), `doc/architecture.md` (committed `12e6756`), `contract.md.tmpl` (inverse-view reference retargeted to `product-map.md`). CHANGELOG + version bump are deferred to `pm-pr-prep` per the plan.
- [x] Expected artifacts exist — plan (`doc/features/product-md-front-door_plan.md`), arch notes (`.ai-pm/arch/product-md-front-door_arch.md`), this review. No Product Contract required (meta-infra).

**DoD: pass**

## Scope

No creep beyond the plan. Backlog #2 (module map) and the general "auto-trigger
migrations" remain out of scope as declared. No leftover path writes the authored
`product.md` via a generator — grep for generator→authored writes returns nothing;
all generation paths target `product-map.md`.

## Blocking

None.

## Notes (product)

None. The split, migration safety, and the worked-example format all match the plan
and the PM-confirmed direction B′.

## Verdict

approve
