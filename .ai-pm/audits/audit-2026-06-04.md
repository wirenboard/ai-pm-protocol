# Protocol audit — 2026-06-04

## Summary

Protocol health is strong. All seven features merged on 2026-06-04 (v2.12.0 → v2.18.0) carry a plan and a review, the eight-agent roster and the `pm-legacy-reader → pm-codebase-reader` rename are clean across the live surface, all five single-source subsections are defined once and referenced by name, and `architecture.md` records a decision for every shipped feature. Two genuine gaps: one review file (blast-radius) carries an **unstamped** `## Code review` section — exactly the bypass the v2.14.0 stamp gate exists to catch — and the architecture File-layout list omits `product.md.tmpl`. Both are cheap to fix. Separately, the backlog has drifted out of currency: several large items shipped this cycle but are not marked resolved.

This is a meta-case audit. This template repo is the **documented no-user-facing-contract exception** (its product is the protocol itself): no `.ai-pm/contracts/`, no `docs/product-map.md`, no `docs/product.md`, no `docs/user-journeys.md` — and it uses `doc/` (singular) for its own dev-docs. None of those absences are findings. The repo is software-kind and non-security, so the dry-run / process and threat-model dimensions are silent here.

## Contract check

Inventory: features merged after the 2026-06-03 audit (which ran 2026-06-04 00:01). Scenario subject extracted from the first sentence of scenario 1 of each `doc/features/<topic>_plan.md`. This repo produces no Product Contracts by design (the meta-exception), so every "needs contract" verdict resolves to n/a regardless of subject — but the extraction is recorded for completeness.

| Feature | Scenario 1 subject | Needs contract | Contract file |
|---|---|---|---|
| on-hardware-blast-radius-preflight (v2.12.0, #183) | the orchestrator (stated: "user of WORKFLOW.md is the orchestrator") | no | n/a (meta-exception) |
| threat-model-ownership-and-lifecycle (v2.13.0, #184) | the orchestrator / agents (stated) | no | n/a (meta-exception) |
| review-stamp-gate (v2.14.0, #185) | the orchestrator / agents (stated) | no | n/a (meta-exception) |
| pm-product-advocate (v2.15.0, #187) | the per-feature gate / the PM-approval flow (mechanism) | no | n/a (meta-exception) |
| legacy-reader-role-split (v2.16.0, #188) | agents / orchestrator / bootstrap process (stated non-human) | no | n/a (meta-exception) |
| doc-migration-on-template-bump (v2.17.0, #190) | a version-keyed manifest (the file) | no | n/a (meta-exception) |
| protocol-process-flavor (v2.18.0, #193) | `/pm-bootstrap` (the command) | no | n/a (meta-exception) |

Every scenario subject is non-human (the orchestrator, agents, a command, a manifest, the bootstrap process). No feature requires a contract on its own merits, and the repo produces none by design. No `yes + MISSING` rows. No advocate artifacts required (no user-facing features). Contract dimension: clean.

## Blocking

1. `.ai-pm/reviews/on-hardware-blast-radius-preflight_review.md` (line 122) — the review carries a `## Code review` section that is a **bare, unstamped heading** (no `: <date> — passed`), preceded by an empty `## Code review findings` heading. **Why it matters:** the v2.14.0 review-stamp gate makes an unstamped `## Code review` section the load-bearing "code review did not happen / was not recorded" signal; `pm-pr-prep` step 0 and `pm-auditor` dimension 1 are required to block on exactly this shape. This feature merged 2026-06-04 00:01 — minutes before the stamp gate itself shipped (v2.14.0, 03:18) — so it is the cohort the backstop is designed to catch: the section is present but the pass was never stamped. **Remediation:** re-run the review-loop Pass 2 for this feature — the orchestrator runs `code-review` over the merged diff and stamps `## Code review: <date> — passed` (or, if the pass genuinely never ran, run it now). Alternatively, if the team treats the pre-stamp-gate cohort as accepted, record it in `.ai-pm/backlog.md` with an `accepted (auditor-2026-06-04)` marker so future audits skip it.

## Notes

1. `.ai-pm/reviews/threat-model-ownership-and-lifecycle_review.md` — the review has **no `## Code review` section at all** (only `## Verdict`, plus a checked "Pipeline green — tests/hooks.sh ran" DoD box). Under the dimension-1 rule, section-absence is the exemption, so this is **not** a blocking finding — but it is the sibling of the blast-radius gap (same pre-stamp-gate cohort, merged 2026-06-04 00:41, before v2.14.0). Flagged as a note so the PM can decide whether the two pre-stamp-gate reviews (blast-radius, threat-model) should be stamped together or accepted as a cohort. **Why it matters:** consistency of the code-review trail across the cohort that shipped just before the stamp gate became load-bearing.

2. `doc/architecture.md` (line 220, File layout → `doc/_templates/`) — the template list omits **`product.md.tmpl`**, which exists on disk (`doc/_templates/product.md.tmpl`). The list correctly added `process.md.tmpl` (process-flavor currency landed) but still does not enumerate `product.md.tmpl`. **Why it matters:** the File-layout map is the protocol's own module map; an omitted shipped template is the same docs-currency drift the protocol exists to catch downstream. This is raised fresh here (not previously flagged in a written audit), so it stays a note. **Remediation:** `pm-architect` adds `product.md.tmpl` to the File-layout template list. (If still missing at the next full audit, it upgrades to blocking.)

3. `doc/architecture.md` (line 5) — `Last full review: 2026-06-03`, while all seven features merged 2026-06-04 and each touched `architecture.md` (its decision records are content-current — verified present for all seven). Only the date field lags. **Why it matters:** minor; the field should reflect the day the records were last updated. **Remediation:** `pm-architect` bumps the date when next editing the file (natural to batch with note 2).

4. `.ai-pm/backlog.md` — backlog currency: several large items shipped this cycle but are not marked resolved, so the PM may re-raise already-done work. **Why it matters:** the PM explicitly asked for this reconciliation; an un-pruned backlog costs re-deciding. Detail below under "Backlog reconciliation". This is a docs/backlog-currency observation, not a protocol-integrity failure.

## Backlog reconciliation (PM's explicit ask)

Resolved-but-not-marked (safe to prune / strike through):

- **"From migration-machinery growth — 2026-06-03"** (extract migration procedures into a standalone reference) — **SHIPPED** as `extract-migrations-reference` → `MIGRATIONS.md`, v2.11.1 (PR #180). Carries no resolution marker. Prune.
- **EPIC "technical-over-product bias" → ANCHOR `pm-product-advocate`** (the missing independent product referee) — **SHIPPED** as `pm-product-advocate`, v2.15.0 (PR #187), with the WORKFLOW Step 3.5 gate, `### Foundational product questions`, the second edit-ownership carve-out, and the `pm-plan-checker` DoD + `pm-auditor` dimension-1 backstops. The epic still describes the anchor as "proposed / sequence first". Mark the anchor resolved; the epic's other sub-items (below) remain.
- **"PM-collaborative documentation migration on template bump — 2026-06-04"** — **SHIPPED** as `doc-migration-on-template-bump`, v2.17.0 (PR #190): the `### Expected-discipline manifest` registry in `MIGRATIONS.md` + the enhanced `/pm-audit` remediation. Mark resolved.

Partially resolved (mark the shipped slice, keep the remainder open):

- **"Protocol for company processes / a process flavor — 2026-06-04 (strategy)"** — the **v1 slice SHIPPED** as `protocol-process-flavor`, v2.18.0 (PR #193): whole-project `software | process` kind, the `### Project kind` single-source rule, `process.md.tmpl`, the dry-run stamp, and the `process` advocate tier. **Still open:** the full per-feature artifact-kind axis (mixed projects) and the SOP-as-stack-note composition path — both explicitly deferred in the plan. Update the note to "v1 shipped; per-feature artifact-kind axis deferred".
- **EPIC sub-item "Bootstrap product/journey asymmetry"** and **"No 'product story fell behind' alarm"** — partially subsumed by the advocate bootstrap pass (v2.15.0) and the doc-migration foundational-journeys discipline (v2.17.0), but the `bootstrap-populated-journeys` plan is **not merged** (deferred). Keep open, with a pointer that the detection half now rides dimension 5 + the doc-migration manifest.

Still open (no action — correctly unshipped):

- "Automation-opportunity scanner (process→code bridge)" — depends on process flavor (now landed); ready for `/pm-research` when picked up.
- "Agent-handoff durability", "State-archive home gap", "Template dev-artifacts must stay inert downstream", "Automode", "README-currency self-exemption" (from threat-model), "Edit-ownership hard guard" (re-opened), and the wb-mqtt-matter downstream-doc items — all genuinely open.
- "From full audit — 2026-06-03": the four pre-protocol-migration plans with no review (`template-v2`, `contract-centric-product-map`, `diagnostic-probe-mode`, `protocol-builtins-realignment`) are already `accepted (auditor-2026-06-03): pre-protocol-migration` — correctly skipped this audit, not re-raised.

## What looks healthy

- **Artifact completeness (recent cohort):** all 7 features have both `doc/features/<topic>_plan.md` and `.ai-pm/reviews/<topic>_review.md`. Five carry a properly stamped `## Code review: 2026-06-04 — passed` (review-stamp-gate, pm-product-advocate, legacy-reader-role-split, doc-migration-on-template-bump, protocol-process-flavor). Only the two pre-stamp-gate features (blast-radius, threat-model) are imperfect (blocking #1 / note #1).
- **Agent roster current:** exactly 8 agent files on disk, matching `architecture.md` line 213 — including `pm-product-advocate` and `pm-codebase-reader`. No dangling `pm-legacy-reader` reference anywhere on the live surface (`.claude/`, `WORKFLOW.md`, `MIGRATIONS.md`, `README.md`, `doc/_templates/`); the rename (v2.16.0) is clean.
- **Single-source integrity intact:** `### Project kind` (WORKFLOW.md:213), `### Expected-discipline manifest` (MIGRATIONS.md:30), `### Foundational product questions` (WORKFLOW.md:249), `### Security-relevant surfaces` (WORKFLOW.md:237), `### Pending-migration detection` (MIGRATIONS.md:7) — each defined exactly once and referenced by name from its consumers; none re-encoded.
- **Architecture.md decision currency:** a recorded decision exists for every one of the 7 features (blast-radius, threat-model, review-stamp gate, pm-product-advocate, legacy-reader rename + journeys ownership, doc-migration manifest, process flavor), and `architecture.md` was touched in every feature merge — so its content is current (only the `Last full review` date field lags, note #3).
- **File-layout template list mostly current:** `process.md.tmpl` is correctly listed; only `product.md.tmpl` is missing (note #2).
- **Plan ↔ implementation parity:** no orphaned implementations — every recent feature-bearing merge maps to a plan; the chore PRs (#186, #189, #191, #192) are backlog/state bookkeeping, not features. Validation is editorial by design (no executable tests except `tests/hooks.sh`, the meta-infra exception), and the recent reviews show `tests/hooks.sh` passing where hooks were touched.

## Priority order for remediation

1. **Resolve the blast-radius unstamped `## Code review` (blocking #1)** — either re-run/stamp Pass 2, or accept the pre-stamp-gate cohort in the backlog. Foundational: this is the one finding that touches protocol-integrity (the gate's own backstop firing on its own repo).
2. **Decide the threat-model review cohort (note #1)** — settle it together with #1 (same pre-stamp-gate cohort).
3. **Add `product.md.tmpl` to the File-layout list + bump `Last full review` (notes #2, #3)** — one `pm-architect` pass over `architecture.md`.
4. **Prune the backlog (note #4)** — mark migration-machinery, the advocate anchor, and doc-migration-on-bump resolved; downgrade the process-flavor strategy note to its deferred remainder.
