# Project audit — 2026-06-02

## Summary

Self-audit of the template repo after the v2.2.0 work (built-ins realignment + contract-centric product map + diagnostic-probe mode). The repo is healthy: the previous audit's blocking findings are closed, the hooks layer is green (`tests/hooks.sh` 65/65), and the new work is internally consistent. The main live debt was README drift — fixed in this same release. No new blocking findings.

## Status of the 2026-05-30 audit findings

| # | 2026-05-30 finding | Status |
|---|---|---|
| 1 | `doc/architecture.md` missing | **Closed** — present and maintained |
| 2 | `doc/stack-notes.md` missing | **Closed** — present |
| 3 | ssh-mut regex under-blocks quoted form | **Closed** — fixed, covered by `tests/hooks.sh` |
| 4 | `find /` boundary under-blocks | **Closed** — fixed, regression-tested |
| 5 | Missing plans for landed work | **Mostly closed** — retroactive/backfill plans landed; ongoing discipline now mechanical |
| 6 | `architect.md` stale `design-review` ref | **Closed** — reworded to "architecture work" |
| 7 | `.gitignore` stale concepts | **Closed** — clean |

## Blocking

None.

## Notes / debts addressed in this pass

1. **README.md drift** — did not reflect v2.2.0 (product map, agent/skill guard, route reminder, diagnostic-probe mode, built-in delegation). **Fixed** in this release: new guarantees, the probe in the production flow, `docs/product.md` in the structure, verify + PR-review in the feature flow.
2. **`docs/product.md` not applicable to this repo** — the template has no user-facing Product Contracts, so a contract-centric map degenerates to a feature list. **Documented as a deliberate exception** in `doc/architecture.md` (contract-map decision). No `product.md` is generated here.
3. **Backlog item (bootstrap full-mode asymmetry)** — already resolved by the legacy-full `pm-architect` Section A handoff. **Closed** in `.ai-pm/backlog.md`.
4. **`*_review.v2.md` files** in `.ai-pm/reviews/` — inspected before touching: these are **not** duplicates but legitimate second-pass re-reviews (a re-check of the fix commits applied after each v1 review), and they cross-reference their v1 files. **Kept** — they are part of the review trail. (The initial "clutter" read in this audit was wrong; corrected on inspection — they were not created by this pass and carry real history.)

## Notes / debts deferred

1. **WORKFLOW.md ↔ README.md ongoing sync** — README (RU, marketing) and WORKFLOW (EN, canonical) must be updated together on every protocol change; this release kept them in sync but the structural coupling remains. Not blocking; revisit if they drift again.
2. **Dogfood gap** — this repo still does not produce per-feature contracts (it is the meta-case, see note 2 above), so its own use of the protocol is necessarily partial. Acceptable.

## What looks healthy

- Hooks layer: 3 PreToolUse + 1 UserPromptSubmit, 65/65 tests, named deny-list with knowledge skills allowed.
- All seven `pm-*` agents tool-locked; only `pm-pr-prep` pins a model (haiku).
- The v2.2.0 plans (`protocol-builtins-realignment`, `contract-centric-product-map`, `diagnostic-probe-mode`) and the analysis doc form a complete paper trail for the session's changes.

## Priority order for remediation

All blocking/notes for this pass are already remediated in the v2.2.0 release. Deferred notes are non-blocking.
