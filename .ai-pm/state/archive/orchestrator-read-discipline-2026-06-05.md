# Execution State

- **Status:** review passed → ready to ship (Step 6 A/B/C gate — awaiting PM)
- **Review:** Pass-1 plan-compliance **approve** (load-bearing no-double-encoding PASS — kernel declared in `WORKFLOW.md:16` only; all by-name refs resolve; core 79→81). Pass-2 `code-review` **zero findings** — stamped `## Code review: 2026-06-05 — passed`. `pm-architect` added the Decision-2 boundary-criterion refinement to `doc/architecture.md` (`### WORKFLOW.md progressive-disclosure boundary` record). `tests/hooks.sh` 74/74.
- **Task:** `orchestrator-read-discipline` — close the orchestrator-side Read-discipline gap from `workflow-progressive-disclosure`: (1) new root `CLAUDE.md` in the template repo that `@`-imports the thin `WORKFLOW.md` core (`@WORKFLOW.md`) + `## Project kind: software` + dogfood note, so the orchestrator developing the protocol auto-loads the thin core/router/invariants; (2) new always-on cross-cutting invariant in the thin `WORKFLOW.md` core — "before applying/citing any `workflow/*.md` rule, Read its file this turn, not from memory". Additive, no migration. Plan: `doc/features/orchestrator-read-discipline_plan.md`. Non-user-facing → no advocate gate. Branch: `feature/orchestrator-read-discipline`.
- **Done (coder), band-aid attempt (`2ef5bc4`):** root `CLAUDE.md` authored (`@WORKFLOW.md` root-relative import + `## Project kind: software` + dogfood note + language canon; ~15 lines, pulls only the thin core) — KEPT, structural prerequisite. The always-on "Read before apply" bullet from that commit has now been replaced by the systemic fix.
- **Done (coder), SYSTEMIC fix (`55c2502`):** moved the decision-authority **kernel** into `WORKFLOW.md` `## Cross-cutting invariants` as one compact always-on bullet — enum (`autonomous | interactive`), `absent OR unrecognized ⇒ interactive` default, derivability test, the 3-trigger escalate-cap (not-derivable / security-surface-on-security-bearing / PM-marked-irreversible-or-high-stakes), announce-before-act, merge-always-manual-in-both-scopes. Deleted those declarations from `workflow/decision-authority.md` (no double-encoding — verified by grep: enum/default/cap/derivability live in the core ONLY; the topic file references the kernel and keeps only the elaboration: value-home file + rationale, per-feature override + resolution order, procedural-gate instances, `## Resolutions` mechanics, veto caveat, consumer list + never-re-encode prohibition). `### Decision authority` heading unmoved → all by-name refs resolve unchanged. Sharpened the core intro with the freeform-vs-procedure boundary criterion (always-loaded, not behind a Read). Demoted "Read before apply" to a trimmed secondary backstop (on-demand elaboration only). Core stays thin (net ~+1 bullet line). `tests/hooks.sh` 74/74. Downstream `@.ai-pm/tooling/WORKFLOW.md` contract + `WORKFLOW.md` path + `doc/_templates/CLAUDE.md.tmpl` byte-unchanged; no `MIGRATIONS.md` / `.claude/settings.json` touched.
- **Done (planning artifacts, `d57bb94`):** revised plan + Variant-A arch note + `doc/architecture.md` File-layout row (CLAUDE.md) committed (owned by pm-plan/pm-architect; orchestrator committed them with the work).
- **Remaining:** `pm-architect` adds the Decision-2 refinement (freeform-vs-procedure criterion) to `doc/architecture.md` decision records (post-coding handoff); review loop; ship.
- **Next step:** review (pm-plan-checker Pass-1 → code-review Pass-2) + pm-architect Decision-2-refinement handoff.
- **Touched files:** `CLAUDE.md` (new, prior commit), `WORKFLOW.md`, `workflow/decision-authority.md`, `doc/features/orchestrator-read-discipline_plan.md` (plan), `doc/architecture.md` (File-layout row), `.ai-pm/arch/orchestrator-read-discipline_arch.md` (arch note), `.ai-pm/state/current.md`.
- **Decision authority:** `autonomous` (project-wide, `.ai-pm/decision-authority.md`) — routine procedural gates announce-and-proceed (v2.24.0); merge/ship stays manual.
- **Last shipped:** `readme-currency` (v2.30.0, PR #236) — per-feature README-currency check in `/pm-plan` + `pm-auditor` README-conformance dimension. Archived: `.ai-pm/state/archive/readme-currency-2026-06-05.md`.
- **Context (this session):** shipped `workflow-progressive-disclosure` (v2.29.0, PR #235) + `readme-currency` (v2.30.0, PR #236). This feature fixes a gap the first one left. Dogfooding finding recorded live: orchestrator applied an on-demand rule (decision-authority escalate-cap) from memory without reading its home — this feature is the structural fix.
- **Deferred:** periodic whole-codebase review (#372) — dropped to do this first; returns afterward, unstarted (nothing branched).
- **EPIC cross-document-consistency auditor — remaining slices:** temporal-status conflation; ADR ↔ stack-notes backing; state-machine ↔ journeys single diagram; journeys ↔ threat-model UX; other NFR classes.
- **PM-flagged queue:** (1) periodic whole-codebase code-quality review (#269/#372); (2) edit-ownership hard-guard + CLAUDE.md-canon auditor duty (#210); (3) deterministic-vs-AI check boundary (#211); (4) cross-model review for high-risk changes; (5) protocol-pipeline-as-Workflow idea.
