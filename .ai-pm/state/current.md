# Execution State

- **Status:** **coding (stage a) — `opencode-harness-support`**. Plan (pass-4) + narrowed arch note both done & committed. PM greenlit stage (a) groundwork. Extraction (stage b) gated on the PM's new-repo target.
  - **SLICE 1 (Claude generator + byte-equivalence) — DONE, REVIEWED, MERGED** into the integration branch (merge `90fa390`; code-review high-effort APPROVE; 2 low-severity non-blocking nits recorded in the merge message — diff-clean orphan gap + test trap under set -u; `tests/generator.sh` 4/4, `tests/hooks.sh` 79/79). `.claude/` is now a generated artifact, byte-identical to the prior committed one (still tracked on the branch).
  - **SLICE 2 (OpenCode adapter — agents/commands/config/AGENTS.md, NON-enforcement) — DONE, REVIEWED, MERGED** (merge `2ab02e4`; code-review high-effort APPROVE; tracked follow-ups: generator.yml not yet running tests/opencode.sh → CI-topology slice; self-contained allowlist for the 2 shared command bodies → install-auto-detect slice). Tests: generator.sh 4/4, opencode.sh 9/9, hooks.sh 79/79.
  - **SPIKE B — RUN LIVE, EXECUTION-VERIFIED (2026-06-07), result POSITIVE.** PM authorized live play with opencode/DeepSeek. Throwaway probe (temp project, deny plugin + `task`-spawned `worker` subagent, DeepSeek model): the plugin `tool.execute.before` hook **DOES fire for and deny a bash call made by a `task`-spawned subagent** — confirmed twice (subagent ran in its own child session `parentID=<main>`, `agent=worker mode=subagent`; HOOK_FIRED+HOOK_DENY on the subagent session; the side-effect `PWNED` file never created). So the disputed #5894 resolves IN OUR FAVOR on **OpenCode 1.16.2 (anomalyco)**: subagent tool-hook containment WORKS. Consequence: the OpenCode enforcement plugin can be **subagent-effective**, and the preview label can state "enforcement verified subagent-effective on 1.16.2" (security-honest caveat: pinned to that version, re-verify on upgrade since #5894 is historically version-sensitive). **Stack-notes tag flip (#5894 rule: `doc-cited (unverified)` → `execution-verified`, version-pinned) is owed to the docs handoff (pm-stack-researcher/pm-architect).** Probe cleaned up.
  - **RUNTIME-CAUGHT DEFECT in slice 2 — FIXED in slice 3.** The generated `.opencode/opencode.json` carried a `_comment` key OpenCode rejects (`Unrecognized key: _comment`). Stripped from the emitted config (source `src/manifests/opencode/opencode.json`); rationale kept in the non-consumed `adapter.json` `_comment`. Verified with the real loader: `opencode debug config --pure` rejects the old config (exit 1) and accepts the new (exit 0). `.claude/settings.json` unaffected.
  - **SLICE 3 (CORE enforcement + config fix) — DONE, on sub-branch `feature/opencode-harness-support--s3-enforcement` (NOT yet reviewed/merged).** Commits: config `_comment` fix + CORE enforcement plugin (`740caef`), AGENTS.md route-reminder + Spike-B preview label + "ask" divergence (`05e54d9`), plugin-registration fix (`e3bf4b9`). Built:
    - **Single-source mechanism (chosen): DERIVE-FROM-settings.json.** The wb-* role-duplicator set has ONE authored copy — the Claude `settings.json` `Task|Agent|Skill` matcher (reproducing its escaped shell case-pattern bytes from a neutral source was judged too risky). The generator EXTRACTS the role list from that matcher at build time and injects it into the OpenCode plugin's `__WB_DENY_ROLES__` placeholder. `settings.json` stays byte-identical to the golden (`generated-claude-adapter-byte-equivalent` intact); no second authored copy; `oc-enforcement-plugin-single-source` test asserts the emitted set == settings.json. New neutral descriptor `src/manifests/deny-list.json` names the CORE rules + records `role_set_source` (it does NOT re-list the roles).
    - **OpenCode CORE enforcement plugin** `.opencode/plugin/ai-pm-enforcement.js` (template `src/manifests/opencode/plugin/ai-pm-enforcement.js.tmpl`): `tool.execute.before` THROWS to deny for (a) wb-* `task` spawn, (b) wb-* `skill` load, (c) `read` outside project root, (d) empty/whitespace `write` over an existing non-empty file. Exports `makeDenyHook(root)` + `WB_DENY_ROLES` for the deterministic unit test. **OpenCode tool/arg mapping (verified against the 1.16.2 runtime binary, NOT only docs):** task->`subagent_type`, skill->`name`, read->`filePath`, write->`filePath`+`content`. NOTE the runtime uses `subagent_type` — contradicting the stack-notes doc claim ("no `subagent_type` string"); surfaced for a stack-notes flip.
    - **Registration = AUTO-LOAD.** Real-loader check showed `.opencode/plugin/*.js` auto-loads; an explicit `opencode.json` `plugin` entry double-registered the same file, so the config carries NO plugin entry (the plugin resolves exactly once; `opencode debug config --pure` exit 0, adapter+8 agents+plugin all load).
    - **"ask"-class guards OUT OF SCOPE (documented divergence).** force-push / commit --no-verify / ssh content-edit / ssh mutating-action are "ask" decisions; `tool.execute.before` has only throw-or-allow (no "ask"), so they map to OpenCode permission config (later slice). Recorded in AGENTS.md; NOT forced into throws.
    - **AGENTS.md:** always-on route-reminder (no UserPromptSubmit-equiv hook — baked into always-on surface; documents always-on vs change-intent-triggered difference); preview label updated to "CORE enforcement verified subagent-effective on OpenCode 1.16.2 (Spike B), version-pinned, re-verify on upgrade"; #5894 now noted verified-in-our-favor on 1.16.2; #17577 stays open; the "ask" divergence documented.
    - **New tests (additive):** `oc-config-valid` (runtime-backed, GUARDED-SKIP when `opencode` absent), `oc-enforcement-plugin-throws` (deterministic unit test `tests/oc-plugin-unit.js`, 8 assertions, GUARDED-SKIP when `node` absent), `oc-enforcement-plugin-single-source` (plugin role set == settings.json, no drift). `tests/opencode.sh` 12/12, `tests/generator.sh` 4/4 (Claude byte-equiv intact), `tests/hooks.sh` 79/79 unchanged.
  - **REMAINING STAGE-(a) SLICES (Spike B done; slice 3 enforcement DONE):** ~~(i) Spike B + OpenCode enforcement plugin~~ **DONE (slice 3 below).** (ii) **WORKFLOW.md/workflow/*.md/doc/_templates generation** from source (completes single-source for the delivered core; currently still hand-authored at root); (iii) **install auto-detect** (+ pays the allowlist debt by neutralizing the bootstrap install prose); (iv) **cross-model frontmatter model pins** (gap #17577); (v) **CI-topology / dist-surface** (largely post-extraction); (vi) **"ask"-class guards via OpenCode permission config** (later — the documented divergence from slice 3). Proves the second harness builds from the SAME neutral bodies via an opencode manifest (no re-carve). Built: `src/manifests/opencode/` (adapter.json + per-agent frontmatter/*.fm in OpenCode shape: `description` + `mode: subagent` + `tools` OBJECT map translated 1:1 from each Claude comma-list grant; opencode.json with `instructions[]` -> `.ai-pm/tooling/WORKFLOW.md`; AGENTS.md source). Generator extended (Claude `settings`→uniform `artifacts` array, output bytes unchanged; `root_relative` for AGENTS.md; `--harness opencode`). Generated tree committed: `.opencode/agent/*` (8), `.opencode/command/*` (5), `.opencode/opencode.json`, root `AGENTS.md`. New `tests/opencode.sh` 9/9. Claude build still byte-identical (`tests/generator.sh` 4/4); `tests/hooks.sh` 79/79. Enforcement plugin + cross-model pins + review/research-engine rebind + install auto-detect are LATER slices; the OpenCode enforcement plugin specifically waits on **Spike B** (subagent containment), proposed before it is built.
    - **SURFACED (scope/categorical — needs a PM/orchestrator call):** the `opencode-adapter-self-contained` invariant is enforced over the adapter's WIRING/LOAD surface (agent frontmatter+body, opencode.json, AGENTS.md — all `.claude/`-free). Two SHARED NEUTRAL command bodies (`pm-audit.body.md`, `pm-bootstrap.body.md`) still carry `.claude/` PROSE (the PreToolUse-guard mention; the Claude symlink-install step) frozen by slice 1. Making the bootstrap install step harness-neutral is the **install-auto-detect slice (OUT OF SCOPE for slice 2)**. Tracked as a tight, documented allowlist in `tests/opencode.sh` (exactly those two files); any NEW cross-read trips the test. NOT resolved by modifying frozen bodies or by adding a non-thin harness-aware body rewrite to the generator.
- **Arch-note outcomes (all resolvable-now design forks settled; recorded in `.ai-pm/arch/opencode-harness-support_arch.md`):** dogfood after split = **hybrid** (daily local build-then-run for immediacy + a dist-pull release-fidelity gate; pure pull-back rejected); CI = version-as-single-token / two tags / tag-only-after-successful-publish split-brain guard / in-source `single-source-diff-clean` + cross-repo byte-equality; identity-inversion map drawn (bulk → source, dist stays lean); sequencing = build+prove on the branch (incl. the dogfood loop, pre-split) → extract later, point-of-no-return = reduce-current-repo-to-dist; thin metalanguage = neutral bodies + per-harness frontmatter manifests + capability→primitive lookup (no abstract DSL); byte-equivalent `.claude/` proven against a frozen golden reference. PM-gated open questions: new-repo target (later), history-preservation at extraction (full history vs snapshot).
- **Decision authority:** `autonomous` (project-wide) — procedural gates announce-and-proceed; merge/ship manual. **Product forks go to the PM.** Conversation language: Russian.
- **Branch + git-flow OVERRIDE for this feature (PM decision 2026-06-07):** treat the integration branch **`feature/opencode-harness-support`** as the **local `main` for the whole OpenCode effort, with NO remote** — everything stays local, **no push, no PR**. **All normal dev rules apply EXCEPT creating a PR / pushing / merging into the repo's real `main`.** Model: cut a **sub-branch off `feature/opencode-harness-support`** per work slice → edit there → run the review loop → **merge back into `feature/opencode-harness-support`** (locally) → repeat until the effort is done. The integration branch **never** merges into the repo's real `main`; the whole effort is later **extracted** into a new source repo (the PM provides the target). Orchestrator operating assumption: planning/arch/design bookkeeping commits go on the integration branch directly; coder slices go on sub-branches merged back. `pm-pr-prep` / Step 6 ship gate is **not used** for this effort.
- **Feature:** add OpenCode as a second supported harness alongside Claude Code. Scope = **design + groundwork slice, staged** (OpenCode adapter as labeled preview), not full certification. Realized via **build-time single-source, two-repo source/dist split (form C)**.
- **PASS-4 PM DECISIONS (2026-06-07) — folded into the rewritten plan:**
  1. **Sync FORM = C, DECIDED** (build-time single-source, two-repo source/dist split). Supersedes the pass-3 "FORM REOPENED, A/B/C undecided" and the earlier lean to A. **A (runtime symlink+pointer) and B (one-repo build) rejected** (A: pointer-softness + Windows symlink fragility; B: generated files mixed into the dev tree).
  2. **Topology:** the **current repo = DISTRIBUTION** (compiled adapters; **submodule URL to downstream UNCHANGED** — consumer sees no change), a **NEW repo = SOURCE** (neutral metalanguage + generator). CI in source builds + publishes to dist.
  3. **Staging:** build neutral source + generator + both adapters **on a branch in the current repo first**; **extract** the source/generator into the new repo **later** (gated on PM target). Extraction is out of this slice's coding.
  4. **"No build step" architecture decision is DELIBERATELY CHANGED** — dev-side build introduced; downstream "plain files, no downstream build" guarantee preserved. Recorded as an amendment in `doc/architecture.md`.
  5. **Three newly-surfaced problems written into the plan as first-class** (designed by the arch note): **identity inversion** of the current repo (dev surface moves out to source), **self-host/dogfood loop change** after the split (build-then-run or pull-back — arch note names the mechanism), **operational cost of C** (cross-repo CI credential, source↔dist version coordination, split-brain guard).
  6. **Submodule correction:** downstream delivery is **unchanged** (same URL, build-free) — it sits in "must not break", not "changes form"; README-currency fires only for the added OpenCode path + harness-support declaration.
- **Prior-pass decisions still in force:** scope = design+groundwork; Claude Code stays self-host, OpenCode = downstream-supported preview; symmetric / no cross-read (`.opencode/` self-contained); single source of truth / no hand-duplicated bodies (now realized by build-time generation); explicit declarative harness-support marker (README + AGENTS.md); `@`-inline confirmed unavailable for agent bodies (moot under C — generator inlines at build time); auto-detect install.
- **Done (orchestrator):**
  - Harness-portability research; `pm-stack-researcher` → `doc/stack-notes.md` § "OpenCode" (8 areas, sourced). Key facts: PR #17577 closed-not-merged (no runtime model override); #5894 closed-disputed (subagent containment, Spike B); no UserPromptSubmit-equiv; `question`/`skill` tools present.
  - Plan authored, then **rewritten pass-4**. State updated.
- **Remaining (no production code yet):**
  1. **Narrowed `pm-architect` arch note** — NOT A/B/C (decided = C). Works out, given C + topology: (1) the dogfood/self-host loop after the split; (2) CI topology + artifact-push + version-sync + split-brain guard; (3) what moves to source vs stays as dist (identity-inversion work item); (4) the migration-on-branch-then-extract sequencing.
  2. Two gated spikes: Spike A = submodule-path sourcing (now topology-sensitive — depends on the arch note's topology output); Spike B = does plugin `tool.execute.before` constrain a `task`-spawned subagent (#5894, topology-independent). **OpenCode runtime IS available in-env** (`opencode 1.16.2` at `~/.opencode/bin/opencode`, node v20.20, npm 10.8) — so the `spike-deferred`-if-no-runtime branch does NOT fire; both spikes can run live (throwaway/local target, PM-authorized). Spike A waits on the arch note (topology); Spike B is independent.
  3. `pm-coder` builds groundwork on sub-branches: neutral source + generator + both generated adapters + diff-clean integrity check + install auto-detect.
     - **SLICE 1 — DONE (Claude side):** golden frozen (`.golden/claude/`, 14 files + SHA256SUMS, commit `d96344c`); harness-neutral source carved for all 13 agent+command bodies + settings.json handled as a Claude-side capability artifact (`src/`, commit `f49df82`); deterministic generator (`gen/generate.py`, thin: inline body + prepend frontmatter + verbatim-copy artifacts); capability→primitive lookup (`src/manifests/capabilities.json`) structured so OpenCode plugs in later without re-carving bodies; live `.claude/` regenerated and byte-identical to committed (still tracked this slice). New tests `tests/generator.sh` (`generated-claude-adapter-byte-equivalent` + `single-source-diff-clean`, verified non-vacuous) + CI `.github/workflows/generator.yml`, commit `7c83ebd`. `tests/hooks.sh` green at 79/79 (untouched).
     - **SLICE 1 — REMAINING (later sub-branches):** the `.opencode/` adapter (agents/commands/plugin/skill/`opencode.json`/`AGENTS.md`); generating `WORKFLOW.md`+`workflow/*.md`; `doc/_templates/` rendering; install auto-detect; dist-surface stub; the two spikes (A submodule-path sourcing, B subagent containment).
  4. Review loop per slice + docs handoff (`doc/architecture.md` two decision records, `README.md`, `AGENTS.md`, `doc/stack-notes.md` tag flips) via `pm-architect`.
  5. **Later, gated:** extraction into the new source repo + CI source→dist wiring (PM provides target).
- **Two upstream blockers tracked (later-slice gates, not closed by us):** runtime model override (#17577) and subagent hook containment (#5894). Full certification is a later plan gated on these.
- **Touched files (slice 1):** `.golden/` (frozen baseline), `src/` (neutral source + manifests), `gen/carve.py` + `gen/generate.py` (carve tool + generator), `tests/generator.sh`, `.github/workflows/generator.yml`, `.ai-pm/state/current.md`. `.claude/` regenerated byte-identical (no diff).
- **Touched files (slice 2):** `src/manifests/opencode/` (adapter.json + 8 frontmatter/*.fm + opencode.json + AGENTS.md source), `src/manifests/claude/adapter.json` (`settings`→`artifacts`, output unchanged), `gen/generate.py` (artifacts array + `root_relative` + `--harness opencode`), generated `.opencode/` (13 files) + root `AGENTS.md`, `tests/opencode.sh` (new), `.ai-pm/state/current.md`. NOT touched: neutral bodies `src/agents|commands/*.body.md`, `src/manifests/capabilities.json` (opencode column was already documented intent in slice 1 — the realized primitives it names are now built; left as-is, no edit needed this slice), `.golden/`, plan/arch/docs. `.github/workflows/generator.yml` NOT extended to opencode.sh yet (CI-wiring nit — flag for review).
- **Touched files (slice 3):** NEW `src/manifests/deny-list.json` (neutral CORE deny descriptor + role_set_source), NEW `src/manifests/opencode/plugin/ai-pm-enforcement.js.tmpl` (plugin template with `__WB_DENY_ROLES__` placeholder), NEW `tests/oc-plugin-unit.js` (deterministic plugin unit test). MODIFIED `gen/generate.py` (`extract_wb_deny_roles()` + plugins-emission step), `src/manifests/opencode/adapter.json` (`plugins` section), `src/manifests/opencode/opencode.json` (`_comment` removed; no plugin entry — auto-load), `src/manifests/opencode/AGENTS.md` (route-reminder + Spike-B label + "ask" divergence), `tests/opencode.sh` (oc-config-valid, oc-enforcement-plugin-throws, oc-enforcement-plugin-single-source). GENERATED `.opencode/plugin/ai-pm-enforcement.js`, `.opencode/opencode.json`, root `AGENTS.md`. NOT touched: neutral bodies `src/agents|commands/*.body.md`, `src/manifests/claude/settings.json` (the role-set single source — left verbatim, byte-identical to golden), `.golden/`, plan/arch/`doc/`. `.github/workflows/generator.yml` still not extended (CI-wiring nit carries over).
- **Next step:** review loop for slice 3 (`pm-plan-checker` Pass 1 + `code-review` Pass 2) on the sub-branch `feature/opencode-harness-support--s3-enforcement` — then merge back into the integration branch. **Docs handoff owed (pm-stack-researcher):** flip `doc/stack-notes.md` § OpenCode #5894 tag `doc-cited (unverified)` -> `execution-verified` (version-pinned 1.16.2) per Spike B; AND record the runtime correction that the OpenCode `task` tool DOES carry a `subagent_type` arg on 1.16.2 (the doc currently claims "no `subagent_type` string"). Then the next groundwork sub-branches: generated `WORKFLOW.md`/`workflow/*.md`/`doc/_templates`; install auto-detect (pays the command-body allowlist debt); cross-model frontmatter pins; CI-topology/dist-surface (largely post-extraction). The "ask"-class guards are a later slice via OpenCode permission config.
- **Out of scope:** the extraction itself (this slice); full certification; closing the two upstream blockers; dogfood-on-OpenCode; form A and form B; hand-duplicated bodies; sibling harnesses (Cursor/Codex/Aider/…); live spike execution if no runtime.
