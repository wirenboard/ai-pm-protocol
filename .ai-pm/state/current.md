# Execution State

> Resume pointer — READ FIRST, by this exact path. Deferred detail: `.ai-pm/backlog.md`. History: commits + CHANGELOG.

**Status (2026-06-12).** `main` = `uni/main` = `ee0a5b0` (**4.10.1**). PRs #25–#31 merged. Ветка чистая.

## What was just shipped
- 4.9.0 (#29) — readability sweep; stamp-gate resilience; builder Visual form; ship-beat close
- 4.10.0 (#30) — continue-a-subagent; reviewer adapter-realization check; fixups
- 4.10.1 (#31) — archaeology sweep; inline-definition terminology; architecture.md "Open assumptions" removed

## Up next
Large items needing design work (check `.ai-pm/backlog.md` for detail):
- `docs/product.md` — product brief for the protocol itself (product discovery)
- `audit` realized — side-tool that runs proactively, not only on demand
- `research` as a doing side-tool with artifacts in `docs/decisions/`
- Product-advocate resume (parked plan)

## Conventions
Russian chat; English artifacts. `interactive` mode. `kind: mixed`, `profile: solo`, `threat-model: rich`. **Remotes:** `uni` (`aadegtyarev/ai-pm-protocol-uni`) — live fork; `origin` OLD. After squash-merge: `git fetch uni && git reset --hard uni/main`.
