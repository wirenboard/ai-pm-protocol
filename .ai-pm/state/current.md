# Execution State

- **Status:** coding — `cross-model-review`. **MODE: repo transfer in progress → work LOCALLY only, NO push / NO PR / NO merge** (PM directive). Plan: `doc/features/cross-model-review_plan.md`.
- **Decision authority:** `autonomous` (project-wide). **Product forks → PM** ("продуктовые решения со мной"). Conversation language: Russian.
- **Stack (local, transfer hold):** `main @ v2.36.0 (MIT)` → `feature/agent-reporting-discipline` (A, done) → `feature/stack-idioms-library` (C, research-parked) → `feature/cross-model-review` (current). Push the whole chain + open PRs once the new remote URL lands.
- **Repo migration:** relicensed MIT (v2.36.0, merged). PM transferring `aadegtyarev/ai-pm-protocol` → `wirenboard` org. **PENDING after transfer:** PM sends new URL → update install-paths (`README.md` ×2 L22/L38, `doc/architecture.md` L387, `.claude/commands/pm-bootstrap.md` L407) → push the local stack + open PRs.
- **This feature — cross-model-review (design LOCKED with PM; default flipped to `auto` 2026-06-05 after cross-model Pass-2):** four settings in `.ai-pm/review-config.md`: `review-diff-model`, `review-full-model`, `audit-model` (each `session|auto|opus|sonnet`, **default `auto`**, absent/unrecognized⇒auto — opinionated cross-model default, rationale in `doc/architecture.md`), `review-scope` (`auto`=all|`high-risk`, default auto; **gates the per-diff path ONLY** — the full sweep uses `review-full-model` regardless). `auto` = review-capable model ≠ session; **Haiku blacklisted** (auto never picks it; not in enum; explicit→refuse). Mechanism: built-in `/code-review` (diff) + orchestrator-skill (full, no own model→pinnable, **fan-out inheritance = verify-before-rely, announce only what's actually pinned**) + `pm-auditor` (audit) run in a **model-pinned subagent**; **no-orchestrator full sweep offers** (a) `/code-review ultra` (cloud, model not ours) or (b) built-in over whole codebase (pinnable); `ultra` is the only non-pinnable path. **No restart** — config read fresh per review. **Command-scoped dialog** ("смени аудитора"→only audit-model). Spike-verified the built-in-in-pinned-subagent mechanism. Downstream migration in MIGRATIONS.md is a **documented default-change note** (non-destructive, announced, reversible with `review-*-model: session`) — **not** an interactive offer.
- **Done (coder):** `workflow/review-typology.md` (`### Cross-model review` single-source rule — four settings, **default `auto`**, absent/unrecognized⇒auto, scope-boundary per-diff-only, Haiku-blacklist, model-pinned-subagent mechanism with fan-out verify-before-rely caveat, no-orchestrator full-model offer, fallback, no-restart, change-dialog + per-review announce) + `WORKFLOW.md` nav-map row + `workflow/pipeline.md` Step 5 Pass 2 (resolve+pin `review-diff-model`) + `.claude/commands/pm-audit.md` (`review-full-model` over the engine chain + scope-does-NOT-apply note + fan-out caveat + no-orchestrator offer + `audit-model` pins the `pm-auditor` spawn) + `.claude/commands/pm-bootstrap.md` (Q9 default `auto` + write `.ai-pm/review-config.md`) + `MIGRATIONS.md` (default-change note, no offer/opt-out/re-nag) + `.ai-pm/review-config.md` (dogfood, all `auto` + `review-scope: auto`) + `workflow/pm-comms.md` (change-dialog/announce reference-by-name). All consumers reference `### Cross-model review` by name — single-source (enum literal lives once in the rule home). `tests/hooks.sh` 73/73.
- **Coder note — `doc/_templates/`:** no `review-config.md.tmpl` and no `CLAUDE.md.tmpl` pointer created — decision-authority (the pattern the plan says to mirror) has neither; its value file is written inline by `/pm-bootstrap`. Following that pattern exactly: the bootstrap inline-write (Q9) IS how the config is "templated"; a separate `.tmpl` would be a second, drift-prone source against single-source discipline.
- **Docs to update (pm-architect, post-coding) — LANDED 2026-06-05:** `doc/architecture.md` (decision record `### Cross-model review`, placed after `### Review-engine selection` as the orthogonal model axis) + `README.md` (cross-model note, sibling of the engine-selection subsection, points at `### Cross-model review`). Both authored by `pm-architect` on `feature/cross-model-review`.
- **Fix round (coder, 2026-06-05) — LANDED:** the 5 cross-model Pass-2 findings + the revised plan (Scenario 1 default `auto`, Scenario 4 no-orchestrator full-model offer, Migration default-change note) implemented across commits `36dc8f7` (default flip to `auto` + scope/fan-out single-source) / `53d7280` (pm-audit scope-boundary + fan-out caveat + no-orchestrator offer) / `99a578b` (migration default-change note + bootstrap auto default). Findings #1/#3/#5 dissolved by the auto-default (no offer machinery); #2 (scope boundary) + #4 (fan-out caveat) fixed in prose. `bash tests/hooks.sh` 73/73.
- **Announce-strengthening (coder, 2026-06-05) — LANDED (PM directive: review/audit model ALWAYS highlighted at invocation):** the "Per-review announce" line in `### Cross-model review` (`workflow/review-typology.md`) hardened from soft "announces the model each time" to **MANDATORY every-path transparency** — never run silently w.r.t. model; holds across all three paths (per-diff / full sweep / compliance audit) and all three model cases (cross-model names the exact model · session-fallback line · ultra "picks its own models"). Single-source (rule home only); consumers (pipeline.md L54, pm-comms.md L28, pm-audit.md L69/L150-153) already reference it by name and stay consistent — no duplication. `bash tests/hooks.sh` 73/73.
- **Next step:** **pm-architect** updates `doc/architecture.md` `### Cross-model review` (still says default `session` + old offer — pm-architect owns it) + `README.md` to match the `auto` default. Then re-stamp the review (Pass-2 line). → **STOP (no push/PR — repo transfer)**.
- **Out of scope:** Pass-1 plan-checker cross-model; custom reviewer; forcing the session model; hard gate/hook.
- **Open queue after this:** C idioms (research done, parked — write `.ai-pm/research/` doc + plan); integration-risk spike gate (#5); seam-completeness; comment-restraint.
- **Dogfood:** clean additive diffs only; agents assert only verified repo facts (reporting-discipline now in force).
