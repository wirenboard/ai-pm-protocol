# Execution State

- **Task:** `documentation-flavor` — generalize the v2.18 `process` flavor → `documentation`: rename kind `process`→`documentation`; open the deliverable (document(s) of any form — md/diagrams/images — in a fixed `deliverable/` dir, distinct from `docs/`); demote `process.md.tmpl` to an optional `doc/_templates/starters/sop.md.tmpl` + add `guide.md.tmpl`; ONE generalized `## Validation: <date> — <method> — passed` stamp (method dry-run|sign-off) absorbing v2.18 `## Dry-run`; ONE general `documentation` foundational-questions tier; back-compat absent-OR-unrecognized ⇒ software + a MIGRATIONS process→documentation rename entry.
- **Status:** review
- **Branch:** `feature/documentation-flavor` (from `main`, post-audit)
- **Done:** plan approved (`doc/features/documentation-flavor_plan.md`); arch review (`.ai-pm/arch/documentation-flavor_arch.md`, Variant A ×6); PM decisions (kind=`documentation`; generalize, keep frame; proceed). Audit 2026-06-04 done (docs current).
- **Done (pm-coder — all 7 items, atomic commits `bc4717b` … `4492919`):**
  1. `bc4717b` — `git mv process.md.tmpl → doc/_templates/starters/sop.md.tmpl` (clean rename diff).
  2. `1d282b2` — opened the deliverable: added `doc/_templates/starters/guide.md.tmpl` (light reference skeleton: purpose/audience/scope/sections/references/revision-history + confirm-against-methodology note); SOP starter prose updated; no mandated deliverable scaffold.
  3. `935a045` — `WORKFLOW.md`: `### Project kind` enum `software | documentation`, default **absent OR unrecognized ⇒ software**, single-sourced `deliverable/` location + diagrams-first-class + images-referenced + optional starters convention; rider, no-code validation discipline (generalized `## Validation: NOT YET RUN`→`<date> — <method> — passed`, method dry-run|sign-off, plan declares it), Pass-2 routing, composition note all `process`→`documentation`; `### Foundational product questions` general `documentation` tier (audience/scope/coverage/decision-points/exceptions+recovery/zero-to-done, branch/recovery N/A-able), tier enum `per-feature | bootstrap | documentation`. Software change-type rows byte-unchanged.
  4. `bd05bb6` — `.claude/commands/pm-bootstrap.md` Q0 `software | documentation` + Documentation-kind scaffolding branch (create `deliverable/`, offer optional starters, dev-docs stack=what/who/tools); `doc/_templates/CLAUDE.md.tmpl` `## Project kind: software | documentation` + `deliverable/` Docs-table row. Software/absent unchanged.
  5. `7af0185` — agents `process`→`documentation` + `## Dry-run`→generalized `## Validation` stamp: pm-coder remit (open deliverable under `deliverable/`, diagrams authored / images referenced); pm-plan-checker born-loud `## Validation: NOT YET RUN` + DoD line + Pass-2 reinterpretation; pm-pr-prep step 0 + pm-auditor dim 1 gate `## Validation` presence-keyed. Software `## Code review` path byte-unchanged.
  6. `31cedcd` — `MIGRATIONS.md` `### Pending-migration detection` entry: stale `## Project kind: process` → one-line rename to `documentation` (the anchor making absent-OR-unrecognized⇒software non-silent); also fixed one residual `## Dry-run` → `## Validation` in pm-pr-prep step-0 prose (line 66).
  7. `4492919` — `README.md` one-line capability mention `process` → documentation projects (SOPs/runbooks/guides/specs/diagrams; open `deliverable/`; doc-type-fit validation).
- **Done (pm-coder — code-review Pass-2 fixes, findings 1+2 from `documentation-flavor_review.md`):**
  - Finding 1(i): `WORKFLOW.md` no-code validation discipline — added "Where the method is declared, and the default" (plan declares method in Test plan / validation section; default by doc type: actionable⇒dry-run, reference⇒sign-off; orchestrator picks safer / asks PM if ambiguous). Single-sourced; load-bearing-gate bullet updated to `## Validation: NOT YET RUN — <method>`.
  - Finding 1(ii): `.claude/agents/pm-plan-checker.md` — born-loud marker now carries the method (`## Validation: NOT YET RUN — <method>`), read from the plan per `### Project kind`; addendum + the two prose references + template comment updated.
  - Finding 2: `.claude/agents/pm-pr-prep.md` step-0 hard rule + `.claude/agents/pm-auditor.md` dim 1 — stamp-format prose now shows full `## Validation: <date> — <method> — passed` (method dry-run|sign-off); `## Code review: <date> — passed` left as-is; grep logic untouched.
  - Tier-name check: found mismatch — `pm-product-advocate.md` listed tiers `per-feature | bootstrap` only, missing the new `documentation` tier (WORKFLOW says advocate is reused verbatim for it). Fixed: tier enum + input line + checklist tier-count + output filename + template `Tier:` line now include `documentation`. WORKFLOW / pm-plan / pm-audit already consistent.
- **Touched files:** `WORKFLOW.md`, `doc/_templates/starters/sop.md.tmpl` (renamed), `doc/_templates/starters/guide.md.tmpl` (new), `doc/_templates/CLAUDE.md.tmpl`, `.claude/commands/pm-bootstrap.md`, `.claude/agents/pm-coder.md`, `.claude/agents/pm-plan-checker.md`, `.claude/agents/pm-pr-prep.md`, `.claude/agents/pm-auditor.md`, `.claude/agents/pm-product-advocate.md`, `MIGRATIONS.md`, `README.md`, `.ai-pm/state/current.md`.
- **Verified:** `grep -rn "Project kind: process|process-kind|## Dry-run|process tier"` → only the intentional `MIGRATIONS.md` detection anchor remains (no live `process` *kind* reference, no `## Dry-run` stamp). `### Project kind` referenced by name by every consumer; enum/default never re-encoded. Software mandatory-table change-type rows byte-unchanged (kind handled by orthogonal rider). `.claude/settings.json` not touched. `doc/architecture.md` not touched (POST-coding, pm-architect). `bash tests/hooks.sh` → 71/71 green.
- **Next step:** review. Spawn `pm-architect` for the `doc/architecture.md` decision (v2.18 process flavor → generalized documentation flavor: kind name, open `deliverable/`, optional SOP/guide starters, doc-type validation, absent-OR-unrecognized⇒software default) — POST-coding handoff — then the review loop (`pm-plan-checker` Pass 1 + `code-review` Pass 2; this meta-feature is software-kind itself → Pass 2 = `code-review`).
- **Validation:** no executable tests (repo constraint); `tests/hooks.sh` green; editorial scenario coverage; optional post-land dogfood (throwaway documentation-kind project: an SOP + a guide).
- **Note:** this generalizes a feature that shipped hours ago (v2.18); back-compat is the load-bearing concern (absent-OR-unrecognized⇒software + the MIGRATIONS rename entry).
