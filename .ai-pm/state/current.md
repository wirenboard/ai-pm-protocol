# Execution State

> Resume pointer — lean by design. Recent steps in brief; older work compressed to one-liners; full detail lives in `doc/features/*_plan.md`, `.ai-pm/arch/`, and the commit log. Keep it this way (don't log every step in full).

- **Task:** OpenCode dual-harness support + token-frugality, dogfooded on THIS repo. Branch `feature/opencode-harness-support` = local main for the effort: **NO remote / NO push / NO PR**; sub-branch → review → merge-back; never merges to the real `main`; later **extracted** to a new source repo (PM provides target). Conversation: Russian. Artifacts: English. **Decision authority: autonomous** (procedural gates announce-and-proceed; product forks → PM; merge/ship manual).

- **Status:** OpenCode adapter functionally complete + faithful — verified LIVE on OpenCode 1.16.2 / DeepSeek, deployed + driving the pipeline correctly in the `nula` downstream (`/home/adegtyarev/Develop/Hobby/nula`).

- **Recent:**
  - **`s15` (built, on sub-branch `feature/opencode-harness-support--s15-plugin-backstop`, NOT merged):** the OpenCode enforcement plugin's STRUCTURAL backstop — two actor/path-aware write guards added to `src/manifests/opencode/plugin/ai-pm-enforcement.js.tmpl` (existing deny cases UNCHANGED; ESM single-export intact). **(f) out-of-root write deny (ALL agents):** `write`/`edit` target + parsed `bash` write-targets (redirect `>`/`>>`, `tee`, `sed -i`, `cp`/`mv` dest, `dd of=`) outside the project root → THROW (closes the `/tmp/nula-dev.log` boundary breach; no actor lookup). **(g) orchestrator content-authoring deny (subagents exempt):** actor resolved via `ctx.client.session.get({path:{id:input.sessionID}})` → `{data:{agent,parentID}}`; primary (no `parentID` / agent `ai-pm`) writing a CONTENT path (in-root but NOT `.ai-pm/**` / `doc/features/**`, not a pure git op) → THROW; subagents (parentID present) exempt; actor lookup FAILS OPEN (miss ⇒ treat as subagent, never a false denial). Closes the live `bash cat > src/…` bypass the removed slice-13 path-permission couldn't. Unit test `tests/oc-plugin-unit.js` extended (14→32 cases) with a MOCK `client.session.get` driving BOTH actors + a fail-open case. `src/manifests/opencode/AGENTS.md` updated (plugin is now the real backstop). LIVE-confirmed on OpenCode 1.16.2/DeepSeek: orchestrator out-of-root write DENIED (f), orchestrator `printf > src/bar.ts` DENIED (g via bash), pm-coder subagent `src/baz.ts` write ALLOWED. Suites: oc-plugin-unit 32/32, opencode form PASS (debug config --pure exit 0), Claude byte-identical 4/4, hooks 79/79, neutral-prose 5/5. Out of scope (noted, not built): the Claude `settings.json` out-of-root write guard (sibling follow-up).
  - **`s15` review-loop FIX PASS (on `feature/opencode-harness-support--s15-plugin-backstop`):** code-review found 2 real defects in the `bashWriteTargets` parser — both fixed in the template + regenerated artifact (byte-equiv). **DEFECT 1 (over-deny):** the redirect/idiom scans matched a `>` that lives only inside a quoted string, an arithmetic `(( a > b ))`, or a `[[ a > b ]]` test — falsely denying legit work incl. the orchestrator's own `.ai-pm` bookkeeping (`printf 'a > b\n' >> .ai-pm/backlog.md` phantom-`b`), and denying `echo "see > /etc/passwd"` for every actor. Fix: new `maskQuotedSpans()` masks quoted `"…"`/`'…'` spans + arithmetic `((…))` + test `[[…]]` spans with same-length `_` filler BEFORE the scans, with a carve-out preserving a quoted token that is itself a redirect TARGET (`echo x > "my file.txt"` still captures the path). **DEFECT 2 (under-deny):** cp/mv destination was the last token of the whole segment, so a trailing redirect (`cp src /out/dest > log`) hijacked the "dest" and the real out-of-root cp target went unchecked. Fix: cut cp/mv args at the first redirect operator (`split(/\d?>>?|</)[0]`) before taking the last token (redirect target already captured by the redirect scan). 7 regression tests ADDED (existing 32 untouched) → oc-plugin-unit 39/39. SKIPPED the optional actor-lookup memoization (review finding 3) — perf-only; would add mutable closure state to a security backstop, not worth it vs. the lazy fail-open lookup. Suites GREEN: oc-plugin-unit 39/39, opencode 28/28 (after commit; diff-clean guard needs the regen committed), generator 4/4 (Claude byte-identical), hooks 79/79, neutral-prose 5/5; `opencode debug config --pure` exit 0.
  - `s16` (merged): per-agent reasoning `variant: minimal` on control agents (token-COUNT lever), single-sourced in the `models` block.
  - **`s17` (built, on sub-branch `feature/opencode-harness-support--s17-context-leanness`, NOT merged):** context-leanness targeted-reading discipline. Single-home rule `### Targeted reading of large structured docs` in `workflow/pipeline.md` (index-first → section → widen); TARGETED bodies (pm-coder/pm-stack-researcher/pm-product-advocate/pm-plan-checker) reference it; FULL carve-outs (pm-architect owner, pm-auditor whole-doc reviewer) read full. Adapters regenerated + Claude golden re-frozen (form A). New `tests/targeted-reading.sh` (7/7). All suites green; neutral-prose clean. Out of scope (untouched): stack-notes targeting, body/always-on shrinking, batching.
  - **`s18` (built, on sub-branch `feature/opencode-harness-support--s18-drop-orch-permission`, NOT merged):** BUGFIX — removed the broken orchestrator path-`permission` block. Diagnosed live in `nula`/OpenCode 1.16.2: the globs `.ai-pm/**`/`doc/features/**` are matched against ABSOLUTE paths so they never matched → every orchestrator write fell through to `**: deny` (denying mandatory `.ai-pm/state|reviews|decision-authority` writes), AND `bash` bypassed it (zero protection). Removed the `permission:` block from `src/manifests/opencode/harness_local/frontmatter/ai-pm.fm` (tools map intact: write/edit/task all true); replaced `oc-orchestrator-write-scoped` → `oc-orchestrator-can-author-own` in `tests/opencode.sh` (asserts write+edit+task granted + NO permission block; guarded-skip runtime = `debug config --pure` exit 0); corrected `src/manifests/opencode/AGENTS.md` wording (persona is enforcement; permission removed; future plugin is backstop); regenerated `.opencode/agent/ai-pm.md` + root `AGENTS.md`. Suites: opencode form+runtime PASS, Claude byte-identical 4/4, hooks 79/79, neutral-prose 5/5. **OPEN — needs orchestrator/pm-architect:** `src/manifests/opencode/harness_local/body/ai-pm.body.md` (the persona, explicitly NOT-touch per the s18 brief) STILL asserts "A per-agent path-permission backstops the rule … do not route around it via bash" — now factually false and actively contradicts the diagnosed need (the orchestrator MUST use bash `cat >>`/`sed -i` for its writes). Body is plan/architect-owned; left untouched, surfaced for a decision.

- **compact-reviewer slices A+B — MERGED to `feature/opencode-harness-support` via `e9e3bc3` (--no-ff, NOT pushed) 2026-06-08.** Review loop closed: Pass-1 plan-compliance approve + Pass-2 code-review approve (deliverable clean — generate.py/configs/generated-agents/Claude-golden all clean; the only findings were 5 test-quality items in `tests/opencode.sh`, all fixed `64004ab`; review stamped `db89f45`). Post-merge suites GREEN (opencode 33/33, generator 4/4, hooks 79/79, neutral-prose 5/5, plugin-unit 39/39, targeted-reading 7/7). **Two non-blocking product notes parked in the review** for the deferred architecture/contract pass: (1) `dual-harness-from-one-source` contract text still lists the retired cross-model pins as a preview gap; (2) the OpenCode `code-review` agent `description` still reads the old thin-engine text (body is now the nine-aspect reviewer). Per-slice detail below.
- **compact-reviewer slice B (built):** control-layer model + single session default + hide build/plan (OpenCode-only; Claude golden byte-identical). `adapter.json` `models` block reduced to ONLY `session` (removed `control`/`control_variant`/`control_agents`); `gen/generate.py` dead pin paths removed (`inject_model_pin`/`inject_variant` deleted, control branches gone) → NO generated agent carries a baked `model:`/`variant:` pin (every agent inherits the session). Shipped `opencode.json` gains `agent.build.disable`+`agent.plan.disable` (picker shows only ai-pm). code-review.fm/deep-research.fm model comments rewritten (no injection). Control-layer model documented as the canonical FOUR-line `agent.<id>.model` personal-config block (PM's own opencode.json, survives a bump) in `doc/stack-notes.md` (new OpenCode §(9), spike facts → execution-verified 2026-06-08) + the AGENTS.md control-layer section; AGENTS.md cross-model section fully rewritten. Tests: `oc-crossmodel-pins`→`oc-single-model-default`, `oc-effort-tier`→`oc-control-layer-model` (+`oc-builtins-hidden`); `oc-orchestrator-can-question` runtime probe re-keyed `build`→`ai-pm` (build now disabled, absent from roster). All runtime guarded checks PASS live on 1.16.2 (debug config --pure exit 0; 4-line override resolves the four checking agents; ai-pm question=allow). Suites green except diff-clean (clears on commit).
- **compact-reviewer review-loop FIX PASS (on `feature/opencode-harness-support--compact-reviewer`):** Pass-2 code-review found 5 test-quality findings, ALL in `tests/opencode.sh` (no shipped artifact touched). All fixed, smallest faithful change each, intent of every existing assertion kept. (1) `oc-compact-reviewer` compactness: added an ABSOLUTE size-ceiling assertion (`wc -c < $CR` < 28000 B) that runs ALWAYS — the only off-machine compactness guard now (the relative `$WBREFS` check is .gitignored, SKIPs on CI); kept the `$WBREFS` < half-source check as a when-present STRONGER bonus; PASS message made honest (always-true claim = the absolute ceiling; wb-relative comparison mentioned only when it ran). Body is 18365 B today, well under the ceiling. (2) `oc-single-model-default` PASS message: escaped the unescaped backticks around `session` (were command-substitution). Swept the slice's other pass/fail messages — only that one defect. (3) the slice-B runtime PASS message reworded to claim only what `debug config --pure` checks (the shipped config parses/loads cleanly) — no longer claims to "verify disables + no pins" at runtime (that's the FORM checks' job). (4) `oc-control-layer-model` runtime override now uses a DISTINCT synthetic id (`dummy-provider/dummy-control-model`, not the session model), so the pass proves the override MOVES the four agents OFF the session — live-verified on 1.16.2 that `--pure` accepts + echoes back an unknown id on all four. (5) `oc-control-layer-model` docs check tightened: replaced the bare anywhere-in-file substring grep with a python block-extractor that scopes to the canonical `"<id>": { "model" ... }` config block and asserts EXACTLY the four ids appear there (missing OR unexpected → fail), robust to prose wording. Suites GREEN: opencode 33/33 (no SKIPs — opencode present, all runtime guards ran) + embedded plugin-unit 39/39, generator 4/4 (Claude byte-identical), hooks 79/79, neutral-prose 5/5, targeted-reading 7/7, oc-plugin-unit 39/39. Diff confined to `tests/opencode.sh`. NOT pushed/merged.
- **compact-reviewer slice A (built + committed `08f42f6` on `feature/opencode-harness-support--compact-reviewer`, NOT merged):** compact one-pass `code-review` engine (OpenCode-only; Claude golden byte-identical). Rewrote `src/manifests/opencode/harness_local/body/code-review.body.md` by compressing the wb `code-review-orchestrator` skill (SKILL.md + 11 briefs, ~58 KB) → a single one-pass reviewer (~18 KB generated, ~32% of source; 131-line body, in the arch note's 120-160 line band). One subagent reads the diff once, reports ALL nine aspects (plan-compliance / security / stability / regressions / test-coverage / conventions / simplification / documentation / architecture) in the shared `<finding>` format. KEPT near-verbatim: 3 severities + confidence, the `<finding>` block, the consolidated-report sections, the verdict rubric incl. the absolute "any plan-compliance deviation → never Approve" row, anchor-to-changed-lines + the global do-not-flag list. INLINED each brief to a dense flag/don't-flag bullet (plan-compliance fullest = hard blocker; project-rules folded into conventions); risk-tier → one agent's internal-effort note (not a fan-out). DROPPED fan-out/spawn orchestration, per-subagent prompt assembly, cross-subagent dedup framing, the skill help block + frontmatter. Convention/doc refs use `AGENTS.md` (not bare `CLAUDE.md`) to stay neutral-prose-clean. New test `oc-compact-reviewer` (mode:subagent, single-pass / no fan-out language, nine-aspect coverage, severities + `<finding>` + report sections + plan-compliance-never-Approve row, substantially-smaller-than-wb-source; guarded-skip runtime `opencode agent list` shows code-review loaded). Suites GREEN (committed): opencode 33/33, generator 4/4 (Claude byte-identical), hooks 79/79, oc-plugin-unit 39/39, neutral-prose 5/5, targeted-reading 7/7; `opencode debug config --pure` exit 0. **Slice C (ultra removal — cross-harness, golden re-freeze) is SEPARATE/out-of-scope here.** Note: an UNRELATED uncommitted `.ai-pm/backlog.md` edit (a pre-existing pipeline-collapse symptom note) was present in the tree and deliberately EXCLUDED from both slice commits.
- **Next:** ✅ compact-reviewer A+B reviewed + merged-back (`e9e3bc3`). Remaining: **slice C** (ultra removal — cross-harness, golden re-freeze) as a separate slice; then the `doc/architecture.md` decision record (post-coding pm-architect handoff, folds the two parked product notes + the s15 backstop record). Also still pending: resolve the `s18` body conflict (the persona body still describes the removed path-permission + a "do not route around via bash" rule that contradicts reality — owner decision), then review `s18` → merge-back. Then review `s17` (pm-plan-checker → code-review) → merge-back. **Extraction stays PM-gated** ("after your real tests" + new-repo target + history-preservation choice).

- **Done — OpenCode effort (one-liners; detail in plans/commits):** s1 Claude generator+byte-equiv (golden) · s2 OpenCode adapter · s3 CORE enforcement plugin+config fix · s4 orchestrator question-seat · s5 plugin ESM-load fix · s7 review/research engines (code-review, deep-research) · s8 find-boundary deny · s9 cross-model pins (session v4-pro / control v4-flash) · s10 neutralize bodies + vocabulary/reference-table + golden re-freeze · s11 neutralize WORKFLOW+workflow/*.md · s12 ai-pm primary orchestrator · s13 orchestrator write path-scope (REVERTED by s18 — the path-permission was broken+bypassable) · s14 orchestrator pipeline-order persona (decision-authority-aware) · s16 effort variant tier. Spikes A (submodule→symlink) + B (#5894 subagent containment WORKS on 1.16.2) execution-verified. Docs/contracts: doc/{product,product-map,user-journeys}.md + 10 Product Contracts (LLM/agent = the user) + architecture decision records + stack-notes execution-verified flips.

- **Deferred / backlog:** extraction (source/dist split) · install-prose neutralization (allowlist debt) · bash-write deny plugin (s15, defense-in-depth — persona holds well live) · "ask"-class enforcement guards · CI source→dist · test-infra no_proxy/stub robustness.

- **nula deploy:** submodule `.ai-pm/tooling`→this build; `.opencode/{agent,command,plugin}` symlinks; root `opencode.json` (default_agent: ai-pm, model v4-pro, instructions [WORKFLOW.md, AGENTS.md], question:allow); `AGENTS.md`→`CLAUDE.md`; plugin npm stub in `.opencode/node_modules` (proxy workaround). All git changes uncommitted for the PM.
