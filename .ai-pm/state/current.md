# Execution State

- **Status:** idle
- **Decision authority:** `autonomous` (project-wide, `.ai-pm/decision-authority.md`) — routine procedural gates announce-and-proceed; merge/ship stays manual. (Kernel now in the `WORKFLOW.md` always-on core as of v2.31.0.)
- **Last shipped:** `orchestrator-read-discipline` (v2.31.0, PR #237) — decision-authority kernel moved into the always-on `WORKFLOW.md` core (single home, no double-encoding) + dogfood root `CLAUDE.md` (`@WORKFLOW.md`) + recorded freeform-vs-procedure boundary criterion. Archived: `.ai-pm/state/archive/orchestrator-read-discipline-2026-06-05.md`.
- **This session shipped:** v2.29.0 `workflow-progressive-disclosure` (WORKFLOW.md → thin core + 15 on-demand `workflow/*.md`, −85% always-loaded) · v2.30.0 `readme-currency` (per-feature README-currency check + `pm-auditor` README-conformance dimension) · v2.31.0 `orchestrator-read-discipline`.
- **Validation note:** v2.31.0 added the template repo's own root `CLAUDE.md` → a **fresh session in this repo now auto-loads the thin `WORKFLOW.md` core + the decision-authority kernel + the boundary criterion**; `workflow/*.md` topic files are read on demand. The next fresh session is the intended validation-by-use of the whole progressive-disclosure + read-discipline structure.
- **Deferred (next candidate):** periodic whole-codebase code-quality review (#372) — engine selection (code-review-orchestrator when available → `/code-review ultra` fallback → `.env` off-switch; per-diff deny intact). Dropped earlier to ship `orchestrator-read-discipline` first; returns unstarted.
- **EPIC cross-document-consistency auditor — remaining slices:** temporal-status conflation; ADR ↔ stack-notes backing; state-machine ↔ journeys single diagram; journeys ↔ threat-model UX; other NFR classes.
- **PM-flagged queue:** (1) periodic whole-codebase code-quality review (#269/#372); (2) edit-ownership hard-guard + CLAUDE.md-canon auditor duty (#210); (3) deterministic-vs-AI check boundary (#211); (4) cross-model review for high-risk changes; (5) protocol-pipeline-as-Workflow idea.
- **Note (state-archive home):** this `current.md` reset + the archive file are **uncommitted on `main`** — committing state to `main` is forbidden, and there is no successor feature branch yet (the known backlog gap "state-archive has no rule-compliant committed home"). They will be committed on the next feature's branch at its Step 0.
