# Execution State

- **Status:** coding — `cross-model-review`. **MODE: repo transfer in progress → work LOCALLY only, NO push / NO PR / NO merge** (PM directive). Plan: `doc/features/cross-model-review_plan.md`.
- **Decision authority:** `autonomous` (project-wide). **Product forks → PM** ("продуктовые решения со мной"). Conversation language: Russian.
- **Stack (local, transfer hold):** `main @ v2.36.0 (MIT)` → `feature/agent-reporting-discipline` (A, done) → `feature/stack-idioms-library` (C, research-parked) → `feature/cross-model-review` (current). Push the whole chain + open PRs once the new remote URL lands.
- **Repo migration:** relicensed MIT (v2.36.0, merged). PM transferring `aadegtyarev/ai-pm-protocol` → `wirenboard` org. **PENDING after transfer:** PM sends new URL → update install-paths (`README.md` ×2 L22/L38, `doc/architecture.md` L387, `.claude/commands/pm-bootstrap.md` L407) → push the local stack + open PRs.
- **This feature — cross-model-review (design LOCKED with PM):** four settings in `.ai-pm/review-config.md`: `review-diff-model`, `review-full-model`, `audit-model` (each `session|auto|opus|sonnet`, **default `session`**, absent⇒session), `review-scope` (`auto`=all|`high-risk`, default auto). `auto` = review-capable model ≠ session; **Haiku blacklisted** (auto never picks it; not in enum; explicit→refuse). Mechanism: built-in `/code-review` (diff) + orchestrator-skill (full, no own model→pinnable) + `pm-auditor` (audit) run in a **model-pinned subagent**; `ultra` is the only non-pinnable path (cloud). **No restart** — config read fresh per review; change applies next review. **Command-scoped dialog** (asks only what the command didn't name; "смени аудитора"→only audit-model). Spike-verified the built-in-in-pinned-subagent mechanism. Downstream migration in MIGRATIONS.md (non-disruptive, absent⇒session).
- **Done (coder):** `workflow/review-typology.md` (`### Cross-model review` single-source rule — four settings, default `session`, absent⇒session, Haiku-blacklist, model-pinned-subagent mechanism, fallback, no-restart, change-dialog + per-review announce) + `WORKFLOW.md` nav-map row (cross-model under review-typology) + `workflow/pipeline.md` Step 5 Pass 2 (resolve+pin `review-diff-model`) + `.claude/commands/pm-audit.md` (`review-full-model` over the engine chain + `audit-model` pins the `pm-auditor` spawn) + `.claude/commands/pm-bootstrap.md` (Q9 + write `.ai-pm/review-config.md`) + `MIGRATIONS.md` (detection entry + setup-offer procedure, absent⇒session) + `.ai-pm/review-config.md` (dogfood, all `session` + `review-scope: auto`) + `workflow/pm-comms.md` (change-dialog/announce reference-by-name). All consumers reference `### Cross-model review` by name — single-source. `tests/hooks.sh` 73/73.
- **Coder note — `doc/_templates/`:** no `review-config.md.tmpl` and no `CLAUDE.md.tmpl` pointer created — decision-authority (the pattern the plan says to mirror) has neither; its value file is written inline by `/pm-bootstrap`. Following that pattern exactly: the bootstrap inline-write (Q9) IS how the config is "templated"; a separate `.tmpl` would be a second, drift-prone source against single-source discipline.
- **Docs to update (pm-architect, post-coding) — LANDED 2026-06-05:** `doc/architecture.md` (decision record `### Cross-model review`, placed after `### Review-engine selection` as the orthogonal model axis) + `README.md` (cross-model note, sibling of the engine-selection subsection, points at `### Cross-model review`). Both authored by `pm-architect` on `feature/cross-model-review`.
- **Next step:** review loop (Pass-1 + Pass-2; **dogfood: this very feature could run Pass-2 cross-model once it exists, but for THIS feature use the normal flow**) → **STOP (no push/PR — repo transfer)**. (pm-architect docs handoff done.)
- **Out of scope:** Pass-1 plan-checker cross-model; custom reviewer; forcing the session model; hard gate/hook.
- **Open queue after this:** C idioms (research done, parked — write `.ai-pm/research/` doc + plan); integration-risk spike gate (#5); seam-completeness; comment-restraint.
- **Dogfood:** clean additive diffs only; agents assert only verified repo facts (reporting-discipline now in force).
