# Execution State

> Resume pointer — lean by design. Recent steps in brief; older work compressed to one-liners; full detail lives in `doc/features/*_plan.md`, `.ai-pm/arch/`, and the commit log. Keep it this way (don't log every step in full).

- **Task:** OpenCode dual-harness support + token-frugality, dogfooded on THIS repo. Branch `feature/opencode-harness-support` = local main for the effort: **NO remote / NO push / NO PR**; sub-branch → review → merge-back; never merges to the real `main`; later **extracted** to a new source repo (PM provides target). Conversation: Russian. Artifacts: English. **Decision authority: autonomous** (procedural gates announce-and-proceed; product forks → PM; merge/ship manual).

- **Status:** OpenCode adapter functionally complete + faithful — verified LIVE on OpenCode 1.16.2 / DeepSeek, deployed + driving the pipeline correctly in the `nula` downstream (`/home/adegtyarev/Develop/Hobby/nula`). **NEW feature PLANNED + approved (2026-06-08): orchestrator anti-corner-cutting** — `doc/features/orchestrator-anti-corner-cutting_plan.md` (pm-plan-checker approve after a 2-gap fix), arch note `.ai-pm/arch/orchestrator-anti-corner-cutting_arch.md`, stack research `doc/stack-notes.md` §(10) message-hooks. Grounded in the nula session `ses_15ce69492ffe` analysis (orchestrator self-substituted a failed advocate verdict + self-authored code + self-merged with hand-set stamps when subagents crashed on the OpenCode SQLite bug). 3 pieces, build order 1→2→3: (1) graceful subagent-failure path [persona-primary: retry N=2 → STOP+report → never self-substitute; "failed=missing"], (2) pre-code/pre-ship artifact-gate [plugin deny: merge when review unstamped + best-effort pm-coder-write when no plan], (3) per-prompt route-reminder via OpenCode chat hooks [OpenCode-only, SPIKE-GATED: model-reach unverified, fallback = always-on instructions]. Pieces 1+2 buildable now (no spike); piece 3 last. NOT user-facing (no advocate gate). Retrospective: an audit is due (last 2026-06-04, >5 features since) — deferred to not derail the explicit plan request. **FEATURE STATUS (2026-06-08): pieces 1+2 SHIPPED+merged (`7b42c30`) + reviewed; `doc/architecture.md` decision record WRITTEN (`5401f57`, pm-architect — the enforcement-point split); piece 3 (chat-hook reminder) SPIKE-DEFERRED — the 2026-06-08 runtime spike was blocked by an `opencode run` startup HANG (the same session-insert SQLite issue), so chat-hook model-reach is unproven-not-refuted → fell back to the always-on instructions surface (which already carries the content via pieces 1+2); re-run the spike where `opencode run` completes a turn before reviving it (stack-notes (10g)/(11)). All pushed to `uni`. So: anti-corner-cutting is DONE as far as buildable now; only piece 3 remains, gated on a working-OpenCode spike.** **PLAN EXTENDED + validated (2026-06-08, `d958911`): "одним махом" — hoist ONE core cross-cutting invariant `Delegation & gate integrity` (generalizes/absorbs Edit-ownership; carve-outs verbatim) into `WORKFLOW.md` (kernel) + `workflow/enforcement.md` (full rule), all corner-cut variants become enforcement instances. Adds 4th variant (stale-artifact reuse — orchestrator presented an existing `audit-*.md` as its own work instead of spawning pm-auditor; pure-behavioural, no env failure; v4-pro NOT weak — accurate self-analysis + self-corrected on one nudge). NEW Piece 0 (core hoist, cross-harness, buildable now, `pm-architect`-owned) + piece-1 persona-echo extension (+named-rationalization ban); provenance gate DEFERRED (feasibility — can't catch read+paraphrase-in-chat). pm-plan-checker: approve (2 non-blocking notes: carve-out safety rests on the architect editorial pass; variant 4 persona-only). Arch note has the EXTENSION + CORE HOIST sections.** **PIECE 0 + piece-1 echo BUILT + reviewed + MERGED (2026-06-08, `40ab1f2`, pushed to `uni`): the always-on Edit-ownership invariant is now generalized to `Delegation & gate integrity` in `WORKFLOW.md` (kernel, with a context-read qualifier so it can't be over-read as forbidding Step-1 reads) + `workflow/enforcement.md` (full rule; both carve-outs + `.ai-pm/state` + the artefact list verbatim; Edit-ownership = the named edit-route instance; fresh-spawn-this-turn test + named-rationalization ban at the rule top). OpenCode persona echoes it (+ stale-artifact behavioral signal). 3 new prose-grep tests (`tests/core-delegation.sh` + `oc-stale-artifact-persona`). Pass-1 (1 contract-ref-drift finding fixed `678e887`) + Pass-2 (3 findings fixed `19dca0a`+`3b867b4`, cross-model Sonnet) both approve. Commits `84adbd6`/`0c4eadb`/`678e887`/`19dca0a`/`3b867b4` → merge `40ab1f2`. **"Сменить инструкции на чёткие" = DONE — the implicit corner-cut rules are now ONE explicit core invariant.** Remaining for the feature: piece 3 (chat-hook reminder, spike-blocked) + the provenance gate (deferred) + folding the core-hoist into the `doc/architecture.md` decision record (post-coding pm-architect, when convenient).**

  - **PIECES 1+2 MERGED to `feature/opencode-harness-support` via `7b42c30` (--no-ff) + pushed to `uni` (feature+main), 2026-06-08.** Review loop closed: Pass-1 + Pass-2 both approve. Pass-2 found 9 findings in the (h) merge-gate + new prose (tests clean): 6 FIXED (`60151f7` — empty/`NOT YET RUN` stamp now denied via a non-empty-content check; gate accepts EITHER `## Code review:` OR `## Validation:` for doc-kind projects; refspec topic terminator extended to `: ~ ^ @`; "pre-code gate denied" prose retitled PERSONA-ONLY; the self-stamp-then-merge over-claim scoped honestly — gate checks stamp PRESENCE not PROVENANCE; `docs/`→`doc/` typo), 3 fail-open ship-path residuals (push `HEAD:main` / merge non-`feature/` branch / detached-HEAD+worktree) DOCUMENTED as known best-effort limits (the "target=main→deny" arm deliberately NOT added — false-deny risk). The (h) gate is honestly a FLOOR: catches the lazy un-stamped/NYR self-merge + the doc-kind case, NOT a hand-fabricated stamp (that residual is persona-scoped + reinforced by piece 1's failure-path discipline, and later piece 3's per-prompt reminder). oc-plugin-unit 39→54. Review `.ai-pm/reviews/orchestrator-anti-corner-cutting-p12_review.md`. Original build detail below. **Remaining for this feature: piece 3 (spike-gated chat-hook reminder) + the `doc/architecture.md` decision record (post-coding pm-architect handoff, now also folds the anti-corner-cutting enforcement split).**
  - **PIECES 1+2 BUILT (2026-06-08, branch `feature/opencode-harness-support--anti-corner-cutting-p12` — now merged, see above).** Piece 3 (chat-hook route-reminder) deliberately OUT OF SCOPE of this sub-branch (spike-gated). **Piece 1 (persona-primary failure path):** universal rule added to `workflow/enforcement.md` (harness-neutral — new `### Graceful subagent-failure` + `### Artifact gate` subsections; neutral-prose clean) AND reinforced in the OpenCode persona body `src/manifests/opencode/harness_local/body/ai-pm.body.md` (new "When a subagent fails" + "Conditional steps are DEFAULT-ON" sections): retry SAME subagent N=2 → STOP+report to PM → NEVER self-substitute verdict/code/stamp/merge; failed/refused = MISSING, never a pass; + the rider (in-doubt→user-facing→advocate; after-coding ALWAYS architect; which-agent-did-I-call self-check; denied-merge = stop, not retry-loop). **Piece 2 (plugin deny):** `src/manifests/opencode/plugin/ai-pm-enforcement.js.tmpl` — **(h) PRE-SHIP MERGE GATE shipped**: orchestrator `git merge`/`git push` of a feature branch whose `.ai-pm/reviews/<topic>_review.md` is absent OR `## Code review:` line missing/`NOT YET RUN` → THROW; topic resolved from `feature/<topic>` in the command or `.git/HEAD`; fail-open on non-feature merge / unresolvable topic / non-orchestrator actor; (a)-(g) byte-behavior-identical, ESM single-export intact. **(i) PRE-CODE write gate DOWNGRADED to persona-only** (documented in the plugin (i) note): no clean structural write→topic signal; the only proxy ("deny when zero plans exist") would contradict the s15 (g) invariant that a subagent authors content legitimately AND is a fragile guess — so the gate stays persona ("a plan precedes code"), backstopped by (h) (a planless/reviewless feature can't SHIP). **Tests:** oc-plugin-unit 39→46 (oc-gate-merge-deny-unstamped, -allow-stamped, -partial-stamp-denied, -bookkeeping-still-allowed, -merge-nonorch-failopen, -precode-no-plan-allowed-by-plugin pinning the downgrade); opencode.sh +3 persona prose-grep (oc-failure-path-persona, persona-conditional-default-on, oc-gate-deny-is-stop-not-loop) → 36 cases (35 pass + the expected pre-commit diff-clean). **Adjacent doc:** `doc/stack-notes.md` new (11) — OpenCode session-insert long-session crash as a known harness limitation (restart to clear; ~293 MB global opencode.db). Did NOT flip (10) to-verify (piece-3 spike gate). Adapters regenerated (`.opencode/agent/ai-pm.md` + plugin); Claude golden byte-identical (workflow/*.md non-generated). Suites GREEN: oc-plugin-unit 46/46, generator 4/4, hooks 79/79, neutral-prose 5/5, targeted-reading 7/7, ultra-absent 2/2, opencode 35/36 (diff-clean clears on commit). **Out of scope (not built):** piece 3, the doc/architecture.md decision record (post-coding pm-architect handoff — DUE next), the (10) to-verify→verified flip.

- **Recent:**
  - **anti-corner-cutting PIECE-1 ECHO + extension tests DONE (2026-06-08, branch `feature/opencode-harness-support--anti-corner-cutting-p0-core`, on top of Piece 0 `84adbd6`; NOT pushed/merged):** persona `src/manifests/opencode/harness_local/body/ai-pm.body.md` gained a new "Delegation & gate integrity" ECHO section (above the existing failure-path/conditional sections) — the unified never-produce/paraphrase/reuse/skip + gate=fresh-spawn-THIS-turn + existing-artifact="not run" statement + the named-rationalization ban (optimize/уже сделано/nothing changed → re-run anyway, never present a stored artifact; audit example) — an ECHO of the WORKFLOW.md/enforcement.md core, not divergent. Regenerated `.opencode/agent/ai-pm.md` (root AGENTS.md is a static manifest copy, no persona body → unchanged); Claude golden byte-identical (generator 4/4). 3 new prose-grep tests: new `tests/core-delegation.sh` (`core-delegation-invariant-present` + `edit-ownership-carveouts-preserved`, 2/2 — pins the WORKFLOW kernel clauses + enforcement full rule + both carve-outs + artefact list), `oc-stale-artifact-persona` added to `tests/opencode.sh`. Suites GREEN: core-delegation 2/2, generator 4/4, neutral-prose 5/5, targeted-reading 7/7, ultra-absent 2/2, hooks 79/79, oc-plugin-unit 54/54, opencode 36/37 (only the expected diff-clean fail, clears on commit). **Pass-2 review-FIX DONE (2026-06-08, NOT pushed/merged): findings 2+3b — persona "What backs this rule" gained the STALE-ARTIFACT signal sentence (parallel to the EDIT-route signal: about to read+relay an existing `audit-*.md`/`_review.md`/plan → stop and spawn fresh); `tests/core-delegation.sh` `edit-ownership-carveouts-preserved` grep loop now asserts the `.ai-pm/state` carve-out token (comment+loop made consistent — fails if `.ai-pm/state` were dropped from enforcement.md's carve-out list). Finding-1 part-1 (WORKFLOW kernel qualifier + state carve-out) already committed by architect `19dca0a`. Regenerated `.opencode/agent/ai-pm.md`; Claude golden byte-identical. Suites GREEN: core-delegation 2/2, generator 4/4, neutral-prose 5/5, targeted-reading 7/7, ultra-absent 2/2, hooks 79/79, oc-plugin-unit 54/54, opencode 37/37 (diff-clean clears on commit).** NEXT: review → merge; doc/architecture.md decision-record extension (pm-architect, post-coding).
  - **Fixup (DONE, NOT pushed/merged) 2026-06-08 — quality-sweep quick-wins (audit findings 1+5), test-internals only, behavior identical:** (1) `tests/opencode.sh` — the orchestrator persona BODY is now extracted ONCE into `$ORCH_BODY` right after `ORCH=` (guarded by `[ -f "$ORCH" ]`); the four body-grep sections (`obody`/`fbody`/`cbody`/`dbody`) reuse it instead of re-running the identical `awk` 4×; the unrelated `crbody` awk (~746, different file `$CR`) left untouched. (2) `tests/oc-plugin-unit.js` — the outer-IIFE `.catch()` now does `fail++; finish()` instead of bare `fs.rmSync+process.exit(1)`, so an unexpected mid-suite throw still prints the `Total: …` summary (happy path unchanged). Diff 15 lines, two test files only, no docs/new-file. Suites GREEN: opencode 36/36, oc-plugin-unit 54/54, generator 4/4, hooks 79/79, neutral-prose 5/5, targeted-reading 7/7, ultra-absent 2/2. Status: done.
  - **MERGE-QUEUE CLEARED (verified 2026-06-08): s15, s17, s18 are ALL already merged into `feature/opencode-harness-support`** (s15 `aba0674`, s17 `528e367`, s18 `66f9285`) — the per-slice "NOT merged" markers below were STALE. The s18 body conflict (persona text describing the removed path-permission / "do not route around via bash") is RESOLVED — no such phrasing remains in `ai-pm.body.md`. Stale sub-branches deleted locally. Per-slice historical detail retained below for the record.
  - **`s15` (MERGED `aba0674`):** the OpenCode enforcement plugin's STRUCTURAL backstop — two actor/path-aware write guards added to `src/manifests/opencode/plugin/ai-pm-enforcement.js.tmpl` (existing deny cases UNCHANGED; ESM single-export intact). **(f) out-of-root write deny (ALL agents):** `write`/`edit` target + parsed `bash` write-targets (redirect `>`/`>>`, `tee`, `sed -i`, `cp`/`mv` dest, `dd of=`) outside the project root → THROW (closes the `/tmp/nula-dev.log` boundary breach; no actor lookup). **(g) orchestrator content-authoring deny (subagents exempt):** actor resolved via `ctx.client.session.get({path:{id:input.sessionID}})` → `{data:{agent,parentID}}`; primary (no `parentID` / agent `ai-pm`) writing a CONTENT path (in-root but NOT `.ai-pm/**` / `doc/features/**`, not a pure git op) → THROW; subagents (parentID present) exempt; actor lookup FAILS OPEN (miss ⇒ treat as subagent, never a false denial). Closes the live `bash cat > src/…` bypass the removed slice-13 path-permission couldn't. Unit test `tests/oc-plugin-unit.js` extended (14→32 cases) with a MOCK `client.session.get` driving BOTH actors + a fail-open case. `src/manifests/opencode/AGENTS.md` updated (plugin is now the real backstop). LIVE-confirmed on OpenCode 1.16.2/DeepSeek: orchestrator out-of-root write DENIED (f), orchestrator `printf > src/bar.ts` DENIED (g via bash), pm-coder subagent `src/baz.ts` write ALLOWED. Suites: oc-plugin-unit 32/32, opencode form PASS (debug config --pure exit 0), Claude byte-identical 4/4, hooks 79/79, neutral-prose 5/5. Out of scope (noted, not built): the Claude `settings.json` out-of-root write guard (sibling follow-up).
  - **`s15` review-loop FIX PASS (on `feature/opencode-harness-support--s15-plugin-backstop`):** code-review found 2 real defects in the `bashWriteTargets` parser — both fixed in the template + regenerated artifact (byte-equiv). **DEFECT 1 (over-deny):** the redirect/idiom scans matched a `>` that lives only inside a quoted string, an arithmetic `(( a > b ))`, or a `[[ a > b ]]` test — falsely denying legit work incl. the orchestrator's own `.ai-pm` bookkeeping (`printf 'a > b\n' >> .ai-pm/backlog.md` phantom-`b`), and denying `echo "see > /etc/passwd"` for every actor. Fix: new `maskQuotedSpans()` masks quoted `"…"`/`'…'` spans + arithmetic `((…))` + test `[[…]]` spans with same-length `_` filler BEFORE the scans, with a carve-out preserving a quoted token that is itself a redirect TARGET (`echo x > "my file.txt"` still captures the path). **DEFECT 2 (under-deny):** cp/mv destination was the last token of the whole segment, so a trailing redirect (`cp src /out/dest > log`) hijacked the "dest" and the real out-of-root cp target went unchecked. Fix: cut cp/mv args at the first redirect operator (`split(/\d?>>?|</)[0]`) before taking the last token (redirect target already captured by the redirect scan). 7 regression tests ADDED (existing 32 untouched) → oc-plugin-unit 39/39. SKIPPED the optional actor-lookup memoization (review finding 3) — perf-only; would add mutable closure state to a security backstop, not worth it vs. the lazy fail-open lookup. Suites GREEN: oc-plugin-unit 39/39, opencode 28/28 (after commit; diff-clean guard needs the regen committed), generator 4/4 (Claude byte-identical), hooks 79/79, neutral-prose 5/5; `opencode debug config --pure` exit 0.
  - `s16` (merged): per-agent reasoning `variant: minimal` on control agents (token-COUNT lever), single-sourced in the `models` block.
  - **`s17` (MERGED `528e367`):** context-leanness targeted-reading discipline. Single-home rule `### Targeted reading of large structured docs` in `workflow/pipeline.md` (index-first → section → widen); TARGETED bodies (pm-coder/pm-stack-researcher/pm-product-advocate/pm-plan-checker) reference it; FULL carve-outs (pm-architect owner, pm-auditor whole-doc reviewer) read full. Adapters regenerated + Claude golden re-frozen (form A). New `tests/targeted-reading.sh` (7/7). All suites green; neutral-prose clean. Out of scope (untouched): stack-notes targeting, body/always-on shrinking, batching.
  - **`s18` (MERGED `66f9285`; body conflict RESOLVED):** BUGFIX — removed the broken orchestrator path-`permission` block. Diagnosed live in `nula`/OpenCode 1.16.2: the globs `.ai-pm/**`/`doc/features/**` are matched against ABSOLUTE paths so they never matched → every orchestrator write fell through to `**: deny` (denying mandatory `.ai-pm/state|reviews|decision-authority` writes), AND `bash` bypassed it (zero protection). Removed the `permission:` block from `src/manifests/opencode/harness_local/frontmatter/ai-pm.fm` (tools map intact: write/edit/task all true); replaced `oc-orchestrator-write-scoped` → `oc-orchestrator-can-author-own` in `tests/opencode.sh` (asserts write+edit+task granted + NO permission block; guarded-skip runtime = `debug config --pure` exit 0); corrected `src/manifests/opencode/AGENTS.md` wording (persona is enforcement; permission removed; future plugin is backstop); regenerated `.opencode/agent/ai-pm.md` + root `AGENTS.md`. Suites: opencode form+runtime PASS, Claude byte-identical 4/4, hooks 79/79, neutral-prose 5/5. (Former OPEN item — the persona body's stale "path-permission backstops the rule … do not route around it via bash" assertion — is RESOLVED: no such phrasing remains in `ai-pm.body.md`.)

- **compact-reviewer slices A+B — MERGED to `feature/opencode-harness-support` via `e9e3bc3` (--no-ff, NOT pushed) 2026-06-08.** Review loop closed: Pass-1 plan-compliance approve + Pass-2 code-review approve (deliverable clean — generate.py/configs/generated-agents/Claude-golden all clean; the only findings were 5 test-quality items in `tests/opencode.sh`, all fixed `64004ab`; review stamped `db89f45`). Post-merge suites GREEN (opencode 33/33, generator 4/4, hooks 79/79, neutral-prose 5/5, plugin-unit 39/39, targeted-reading 7/7). **Two non-blocking product notes parked in the review** for the deferred architecture/contract pass: (1) `dual-harness-from-one-source` contract text still lists the retired cross-model pins as a preview gap; (2) the OpenCode `code-review` agent `description` still reads the old thin-engine text (body is now the nine-aspect reviewer). Per-slice detail below.
- **compact-reviewer slice B (built):** control-layer model + single session default + hide build/plan (OpenCode-only; Claude golden byte-identical). `adapter.json` `models` block reduced to ONLY `session` (removed `control`/`control_variant`/`control_agents`); `gen/generate.py` dead pin paths removed (`inject_model_pin`/`inject_variant` deleted, control branches gone) → NO generated agent carries a baked `model:`/`variant:` pin (every agent inherits the session). Shipped `opencode.json` gains `agent.build.disable`+`agent.plan.disable` (picker shows only ai-pm). code-review.fm/deep-research.fm model comments rewritten (no injection). Control-layer model documented as the canonical FOUR-line `agent.<id>.model` personal-config block (PM's own opencode.json, survives a bump) in `doc/stack-notes.md` (new OpenCode §(9), spike facts → execution-verified 2026-06-08) + the AGENTS.md control-layer section; AGENTS.md cross-model section fully rewritten. Tests: `oc-crossmodel-pins`→`oc-single-model-default`, `oc-effort-tier`→`oc-control-layer-model` (+`oc-builtins-hidden`); `oc-orchestrator-can-question` runtime probe re-keyed `build`→`ai-pm` (build now disabled, absent from roster). All runtime guarded checks PASS live on 1.16.2 (debug config --pure exit 0; 4-line override resolves the four checking agents; ai-pm question=allow). Suites green except diff-clean (clears on commit).
- **compact-reviewer review-loop FIX PASS (on `feature/opencode-harness-support--compact-reviewer`):** Pass-2 code-review found 5 test-quality findings, ALL in `tests/opencode.sh` (no shipped artifact touched). All fixed, smallest faithful change each, intent of every existing assertion kept. (1) `oc-compact-reviewer` compactness: added an ABSOLUTE size-ceiling assertion (`wc -c < $CR` < 28000 B) that runs ALWAYS — the only off-machine compactness guard now (the relative `$WBREFS` check is .gitignored, SKIPs on CI); kept the `$WBREFS` < half-source check as a when-present STRONGER bonus; PASS message made honest (always-true claim = the absolute ceiling; wb-relative comparison mentioned only when it ran). Body is 18365 B today, well under the ceiling. (2) `oc-single-model-default` PASS message: escaped the unescaped backticks around `session` (were command-substitution). Swept the slice's other pass/fail messages — only that one defect. (3) the slice-B runtime PASS message reworded to claim only what `debug config --pure` checks (the shipped config parses/loads cleanly) — no longer claims to "verify disables + no pins" at runtime (that's the FORM checks' job). (4) `oc-control-layer-model` runtime override now uses a DISTINCT synthetic id (`dummy-provider/dummy-control-model`, not the session model), so the pass proves the override MOVES the four agents OFF the session — live-verified on 1.16.2 that `--pure` accepts + echoes back an unknown id on all four. (5) `oc-control-layer-model` docs check tightened: replaced the bare anywhere-in-file substring grep with a python block-extractor that scopes to the canonical `"<id>": { "model" ... }` config block and asserts EXACTLY the four ids appear there (missing OR unexpected → fail), robust to prose wording. Suites GREEN: opencode 33/33 (no SKIPs — opencode present, all runtime guards ran) + embedded plugin-unit 39/39, generator 4/4 (Claude byte-identical), hooks 79/79, neutral-prose 5/5, targeted-reading 7/7, oc-plugin-unit 39/39. Diff confined to `tests/opencode.sh`. NOT pushed/merged.
- **compact-reviewer slice A (built + committed `08f42f6` on `feature/opencode-harness-support--compact-reviewer`, NOT merged):** compact one-pass `code-review` engine (OpenCode-only; Claude golden byte-identical). Rewrote `src/manifests/opencode/harness_local/body/code-review.body.md` by compressing the wb `code-review-orchestrator` skill (SKILL.md + 11 briefs, ~58 KB) → a single one-pass reviewer (~18 KB generated, ~32% of source; 131-line body, in the arch note's 120-160 line band). One subagent reads the diff once, reports ALL nine aspects (plan-compliance / security / stability / regressions / test-coverage / conventions / simplification / documentation / architecture) in the shared `<finding>` format. KEPT near-verbatim: 3 severities + confidence, the `<finding>` block, the consolidated-report sections, the verdict rubric incl. the absolute "any plan-compliance deviation → never Approve" row, anchor-to-changed-lines + the global do-not-flag list. INLINED each brief to a dense flag/don't-flag bullet (plan-compliance fullest = hard blocker; project-rules folded into conventions); risk-tier → one agent's internal-effort note (not a fan-out). DROPPED fan-out/spawn orchestration, per-subagent prompt assembly, cross-subagent dedup framing, the skill help block + frontmatter. Convention/doc refs use `AGENTS.md` (not bare `CLAUDE.md`) to stay neutral-prose-clean. New test `oc-compact-reviewer` (mode:subagent, single-pass / no fan-out language, nine-aspect coverage, severities + `<finding>` + report sections + plan-compliance-never-Approve row, substantially-smaller-than-wb-source; guarded-skip runtime `opencode agent list` shows code-review loaded). Suites GREEN (committed): opencode 33/33, generator 4/4 (Claude byte-identical), hooks 79/79, oc-plugin-unit 39/39, neutral-prose 5/5, targeted-reading 7/7; `opencode debug config --pure` exit 0. **Slice C (ultra removal — cross-harness, golden re-freeze) is SEPARATE/out-of-scope here.** Note: an UNRELATED uncommitted `.ai-pm/backlog.md` edit (a pre-existing pipeline-collapse symptom note) was present in the tree and deliberately EXCLUDED from both slice commits.
- **EXTRACTION DONE (2026-06-08) — new home repo `git@github.com:aadegtyarev/ai-pm-protocol-uni.git` (remote `uni`).** PM provided the target. Branch `feature/opencode-harness-support` (full 412-commit history from `v0`, PM-chosen: keep branch name + full history) pushed to `uni`; then force-pushed to `uni` `main` (overwrote the GitHub initial-commit stub `0b1b253`) so the default branch carries the protocol and is submodule-installable. All three `uni` refs at `173f048`. The OLD `origin` (`aadegtyarev/ai-pm-protocol.git`) untouched. Local commit-author email set to `adegtyarev.ap@gmail.com` for this repo (future commits only).
- **Fixup (done, `58b4969`; reviewed approve `173f048`; PUSHED to uni):** repointed the template clone/submodule install URL `wirenboard/ai-pm-protocol.git` → `aadegtyarev/ai-pm-protocol-uni.git` (README ×3, pm-bootstrap source, doc/architecture sentence); regenerated Claude+OpenCode adapters + re-froze Claude golden; protocol NAME + chore-labels untouched; zero `wirenboard/ai-pm-protocol` left in tracked .md; all suites green (generator 4/4, hooks 79/79, neutral-prose 5/5). Trivial-mode verdict `.ai-pm/reviews/fixup-install-url_review.md`. So `uni` main now self-references the correct install URL.

- **compact-reviewer slice C — BUILT + committed (`901f505` on `feature/opencode-harness-support--slice-c-ultra-removal`, NOT pushed/merged) 2026-06-08.** Cross-harness ultra removal + sweep-fallback rewrite (plan pieces 2+3). Piece 3: `ultra` retired as a review LEVEL everywhere it was a live protocol instruction — `workflow/review-typology.md` (smell/hygiene depth low→ultra → **low→high**; functional/integration + seam "ultra AI"/"ultra depth" intensity → high/heaviest; det-vs-AI low→ultra → low→max; the **Engine selection block rewritten harness-neutral** — per-diff = the per-diff review engine = compact reviewer where no built-in / built-in `code-review` where one exists; the `### Cross-model review` no-orchestrator `(a) ultra vs (b) built-in` fork **collapses to a single pinnable whole-tree path**; the per-review-announce ultra case removed), `workflow/roster.md` (dropped the "use ultra level" sentence + the `/pm-audit` "optional code-review ultra"; engine line restated as orchestrator-preferred / compact-reviewer-fallback by reference), `src/commands/pm-audit.body.md` (depth menus + list ultra-free low/medium/high/max; no-orchestrator offer collapses to the single pinnable per-diff-engine-over-the-whole-tree fallback), `README.md` (Russian — fallback = compact reviewer / built-in over the whole tree; ultra gone). Piece 2: the sweep-fallback rule single-sourced in `review-typology.md` (orchestrator NOT installed → per-diff review engine over the whole tree, NOT `/code-review ultra`; orchestrator stays preferred when present), referenced by `pm-audit`. Regenerated both adapters (`.claude/`+`.opencode/` pm-audit.md) + **re-froze the Claude golden** (`.golden/claude/` pm-audit.md + SHA256SUMS) — only pm-audit.md changed per adapter (the `workflow/*.md` + README are non-generated root files). New `tests/ultra-absent.sh` — whole-word clean-grep over the **git-tracked** scoped surfaces (workflow/, src/commands/, README.md, .claude/, .opencode/, .golden/; git-tracked scoping skips the untracked `.opencode/node_modules` vendored stub) + a non-vacuous reintroduction-trips self-check. Suites GREEN: generator 4/4 (Claude byte-identical + diff-clean), opencode 33/33 (+embedded plugin-unit 39/39), ultra-absent 2/2, hooks 79/79, neutral-prose 5/5, oc-plugin-unit 39/39, targeted-reading 7/7.

- **code-review agent `description` refresh (DONE 2026-06-08, NOT pushed/merged):** the OpenCode `code-review` agent `description` frontmatter was STALE (old thin two-dimension engine text) while the body is the nine-aspect compact reviewer. Updated ONLY the `description` field in `src/manifests/opencode/harness_local/frontmatter/code-review.fm` to a one-liner reflecting the nine aspects + three severities + structured `<finding>` + consolidated verdict + plan-compliance hard blocker (sibling-description style; model comment + all other fields untouched). Regenerated `.opencode/agent/code-review.md` (only the description line changed); Claude golden untouched (OpenCode-only agent). No `tests/opencode.sh` assertion pinned the old text (the assertions check body aspects/finding-block/verdict + a byte ceiling). Resolves the second parked product note. Suites GREEN: generator 4/4 (Claude byte-identical), opencode 33/33, hooks 79/79, neutral-prose 5/5, oc-plugin-unit 39/39, targeted-reading 7/7, ultra-absent 2/2.
- **compact-reviewer slice C — Pass-2 review-fix pass DONE (on `feature/opencode-harness-support--slice-c-ultra-removal`, NOT pushed/merged) 2026-06-08.** Pass-2 code-review found 3 real defects; all fixed, smallest faithful change each. **FINDING C (prose, `workflow/review-typology.md:10`):** the smell/hygiene depth was `low→high`, one notch short of the canonical ladder (`pm-audit` runner enumerates low/medium/high/max; line 17/24 say `low→max`); fixed `low→high`→`low→max`; confirmed no other `low→high`/`low→ultra` depth ladder remains in scope. **FINDING A (test false-pass, `tests/ultra-absent.sh` `scan_ultra()`):** `git ls-files -z | xargs grep` had no `--no-run-if-empty`, so an empty scoped surface let grep read inherited STDIN and pass vacuously; fixed by (i) detecting zero tracked-files-in-scope and returning rc 2 = HARD FAIL (the scoped paths are expected to exist), (ii) `--no-run-if-empty` on xargs as a defensive stdin-leak stop, (iii) emptiness counted via plain `git ls-files | wc -l` (never storing the `-z` NUL stream in a shell var — command substitution strips NULs and broke the `-0` pairing). **FINDING B (self-check bypassed plumbing, `tests/ultra-absent.sh` non-vacuous check):** the old check ran `grep` on a scratch file — proved the regex, not the production scan; rewrote it to inject an `ultra` review-level phrase into a tracked in-scope file (README.md), run the REAL `scan_ultra()` (git ls-files|xargs grep), assert it reports the injection, restore via `git checkout` under a trap (tree never left dirty). Verified the self-check FAILS when scan_ultra() is broken. Pipeline ALL GREEN: ultra-absent 2/2, generator 4/4 (Claude byte-identical + diff-clean — `review-typology.md` is non-generated), hooks 79/79, neutral-prose 5/5, opencode 33/33 (+plugin-unit 39/39), oc-plugin-unit 39/39, targeted-reading 7/7.
- **anti-corner-cutting-p12 Pass-2 review-FIX pass DONE (2026-06-08, NOT pushed/merged):** code-review found 6 real defects in the (h) merge gate + enforcement prose (tests/persona clean — untouched). Plugin `reviewStampSatisfied` now requires NON-EMPTY/not-NYR content (empty `## Code review:` heading no longer false-allows) AND accepts the documentation-kind `## Validation:` stamp (no false-deny of a validated merge); `resolveMergeTopic` terminator extended to stop at `:~^@` (refspec `feature/foo:main` -> topic `foo`); residual fail-open ship-path bypasses documented (HEAD:main / non-feature merge / detached HEAD / worktree — NOT a "target=main" deny, would false-deny legit syncs). enforcement.md: Artifact-gate retitled "pre-code = PERSONA-ONLY (no plugin deny)"; self-stamp claim scoped to presence-not-provenance (fabrication stays persona-only, distinct from the legit Pass-2 stamp); merge-gate-is-a-floor one-liner; two diff-introduced `docs/`->`doc/` path fixes. +8 unit tests (46->54). Suites GREEN: oc-plugin-unit 54/54, opencode 35/36 (diff-clean clears on commit), generator 4/4, hooks 79/79, neutral-prose 5/5, targeted-reading 7/7, ultra-absent 2/2.
- **Next / STATUS — FEATURE COMPLETE (autopilot, 2026-06-08):** ✅ compact-reviewer all 5 pieces built + reviewed (Pass-1+Pass-2) + merged-back (`e9e3bc3`, `73bdbed`); ✅ merge-queue cleared (s15/s17/s18 already merged); ✅ extraction to `uni` + install-URL repoint; ✅ `doc/architecture.md` decision record written (`a588361`, pm-architect — records all 5 pieces + retires the stale in-doc `ultra` mentions + new-home note); ✅ both parked product notes RESOLVED — `dual-harness-from-one-source` contract cross-model text fixed (`a588361`), OpenCode `code-review` agent `description` refreshed to the nine-aspect compact reviewer (`40132de`). **Whole OpenCode dual-harness effort + compact-reviewer feature is DONE on the integration branch; all suites green (generator 4/4, opencode 33/33, hooks 79/79, neutral-prose 5/5, plugin-unit 39/39, targeted-reading 7/7, ultra-absent 2/2); pushed to `uni` (feature+main).** Awaiting the PM's REAL/runtime tests (PM: "потом у нас будут тесты"). NOTE: keep `uni` updated on future merges. Open follow-ups remain only in `.ai-pm/backlog.md` (e.g. the orchestrator corner-cutting / chat.message route-reminder item; extraction source/dist split; install-prose neutralization) — none block this feature.

- **Done — OpenCode effort (one-liners; detail in plans/commits):** s1 Claude generator+byte-equiv (golden) · s2 OpenCode adapter · s3 CORE enforcement plugin+config fix · s4 orchestrator question-seat · s5 plugin ESM-load fix · s7 review/research engines (code-review, deep-research) · s8 find-boundary deny · s9 cross-model pins (session v4-pro / control v4-flash) · s10 neutralize bodies + vocabulary/reference-table + golden re-freeze · s11 neutralize WORKFLOW+workflow/*.md · s12 ai-pm primary orchestrator · s13 orchestrator write path-scope (REVERTED by s18 — the path-permission was broken+bypassable) · s14 orchestrator pipeline-order persona (decision-authority-aware) · s16 effort variant tier. Spikes A (submodule→symlink) + B (#5894 subagent containment WORKS on 1.16.2) execution-verified. Docs/contracts: doc/{product,product-map,user-journeys}.md + 10 Product Contracts (LLM/agent = the user) + architecture decision records + stack-notes execution-verified flips.

- **Deferred / backlog:** extraction (source/dist split) · install-prose neutralization (allowlist debt) · bash-write deny plugin (s15, defense-in-depth — persona holds well live) · "ask"-class enforcement guards · CI source→dist · test-infra no_proxy/stub robustness.

- **nula deploy:** submodule `.ai-pm/tooling`→this build; `.opencode/{agent,command,plugin}` symlinks; root `opencode.json` (default_agent: ai-pm, model v4-pro, instructions [WORKFLOW.md, AGENTS.md], question:allow); `AGENTS.md`→`CLAUDE.md`; plugin npm stub in `.opencode/node_modules` (proxy workaround). All git changes uncommitted for the PM.
