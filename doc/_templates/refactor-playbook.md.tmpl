---
skip_eligibility:
  default: optional
  skip_if: "greenfield без legacy"
---

# Refactor Playbook (deprecated as standalone — moved to maintenance-playbook.md Part B)

**v0.3.0 redirect.** Refactor playbook больше не отдельный артефакт. Содержимое теперь живёт как **Part B в `maintenance-playbook.md`** (same axis — maintainability).

## Migration

1. Скопировать filled content в `maintenance-playbook.md` Part B (sections B.1-B.9)
2. Удалить этот файл из `doc/`

`template-sync-doc-migrate.py` автоматизирует.

См. `meta/design/2026-05-25_merged-optimization-plan.md` § v0.3.0 Path C.
