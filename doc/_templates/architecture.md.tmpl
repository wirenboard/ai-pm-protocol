# Architecture

## External standards

Company or team standards that apply to this project. Agents read these as hard constraints — same weight as architectural decisions.

- **Approved tech stack:** <e.g., "Backend: Python only — company standard. Frontend: React or vanilla JS.">
- **Forbidden without approval:** <e.g., "No new databases without architecture review. No GPL dependencies.">
- **Codestyle guide:** <link or inline key rules — e.g., "https://company.internal/codestyle/python">
- **Required tooling:** <e.g., "pylint + mypy + black. CI must pass before merge.">

If no company standards apply — remove this section.

---

## Tech stack

| Layer | Choice | Why |
|---|---|---|
| Language | <...> | <reason> |
| Framework | <...> | <reason> |
| Database | <...> | <reason> |
| Transport | <...> | <reason> |
| <other> | <...> | <reason> |

## Architectural decisions

Document decisions that are not obvious from the stack choice. Each decision: what was chosen, why, what was rejected and why.

### <Decision 1 title>

**Chosen:** <option>
**Why:** <reason>
**Rejected:** <alternative> — because <reason>

### <Decision 2 title>

...

## Architectural constraints

Things that must not be violated without an explicit PM decision. Agents read this and treat constraints as hard boundaries.

- <e.g., "Only Postgres — no additional databases. Reason: operational simplicity, single backup target.">
- <e.g., "No external services in hot path — everything must work offline. Reason: embedded deployment.">
- <e.g., "No background threads outside the worker pool — reason: reproducibility in tests.">

## File layout (module map)

Coarse map of the developer/agent-facing source structure: **one row per top-level directory or module → one-line responsibility**. Stay coarse — NOT per-function, NOT a file listing (that drifts the moment code moves). This is the `src/` structure on the *responsibility* axis, distinct from `docs/product.md` "## Документы" (which is PM-facing navigation over `docs/`). This is the section `pm-architect` (A4) cross-checks against `ls` + `git ls-tree -r --name-only HEAD`, and the section `pm-auditor` keys its "major components listed?" check on — keep it honest against the real tree.

| Directory / module | Responsibility |
|---|---|
| `<src/api/>` | <one-line responsibility — e.g., "HTTP handlers, request validation"> |
| `<src/core/>` | <one-line responsibility — e.g., "domain logic, no I/O"> |
| `<src/store/>` | <one-line responsibility — e.g., "persistence, queries"> |

## Integration contract

How downstream / external consumers integrate with this project — the public surface they depend on (install steps, entry points, exported API, expected paths, env contract). Keep it aligned with the README install instructions: `pm-architect` (A4) cross-checks this section against them, and a diverging description is a self-inflicted finding. Mark `N/A — <reason>` for projects with no external integration surface.

- <e.g., "Installed via `pip install <pkg>`; entry point `<pkg>.run(config)`; reads config from `$XDG_CONFIG_HOME/<pkg>/config.toml`.">

## Release flow

How a commit becomes a published release. Keep it aligned with the CI definition: `pm-architect` (A4) cross-checks this section against `.github/workflows/auto-tag.yml` (or the equivalent CI workflow). Mark `N/A — <reason>` for projects with no automated release.

- <e.g., "Feature branch → PR → squash-merge to main → CI reads top CHANGELOG entry → tags `v<VERSION>` and publishes a GitHub Release.">

## Security constraints

- <e.g., "Passwords: bcrypt min cost 12, never stored plain, never logged">
- <e.g., "API keys: env vars only, never in source, never in response bodies or headers">
- <e.g., "User input: validated and sanitized before any DB query or shell invocation">
- <e.g., "Tokens: short-lived, not stored client-side beyond session">

## Code conventions

### AI-specific minimums

These exist because AI tends to write monolithic files and complex functions. Adjust if project has legitimate exceptions (generated code, data files).

- Max source file: 300 lines
- Max function / method: 50 lines
- Cyclomatic complexity: max 10 per function
- No file-level lint suppressions (only line- or function-level with comment)
- New code test coverage: min 80%

### Stack-specific rules

<e.g., naming conventions, error handling patterns, import organization — specific to this project's stack>

### Linter commands

```
<lint command>
<format command if separate>
```

## Dependencies

### Policy

<e.g., "Prefer standard library. Add a dependency only if it saves >100 lines of non-trivial code and is actively maintained.">

### Current dependencies

| Package | Purpose | Added |
|---|---|---|
| <name> | <why> | <date or feature> |
