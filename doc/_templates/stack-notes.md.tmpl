<!-- Doc-style rules apply: fact-first, current-state-only, provenance-as-pointer, plain-language. Read them in `## Durable-text frugality` in `workflow/doc-style.md` (`.ai-pm/tooling/workflow/doc-style.md` once installed) — the authority; this comment names the rules and points there, it does not restate them. (Stack-notes-local: provenance here is one `Source:` line per rule.) -->

# Stack notes

Living document. Initialised at bootstrap, extended on every feature that touches a new external system.
Maintained by `pm-stack-researcher`. Read by `pm-plan`, `pm-architect`, `pm-coder`, `pm-plan-checker`.

**Last full review:** <YYYY-MM-DD>

---

## How this document is used

- **pm-plan** reads it before drafting a plan that touches any listed component. If the feature touches a component that is missing here, `/pm-plan` spawns `pm-stack-researcher` to extend this document **before** continuing.
- **pm-architect** reads it when proposing variants — stack constraints are part of the trade-off space.
- **pm-coder** reads it before writing a mapping, handler, schema, or any integration code for a listed component. On contradiction between task and stack-notes, coder stops and escalates — no fallback to WebSearch.
- **pm-plan-checker** checks every diff against the relevant entries. Code that contradicts an idiom or constraint listed here is **blocking** with a citation back to this file.

If this document is missing or empty for a component the feature touches — that is a protocol-level defect, not a content gap. `/pm-bootstrap` or `/pm-plan` should have caught it.

---

## Platform filesystem layout

> **Fill in this section before any packaging, deployment, or config-placement work.** Agents use it to verify path choices. If the platform has no special filesystem constraints, write "N/A — standard Linux FHS, no partition survival rules" so reviewers know it was considered.

- **Target platform:** <e.g. Wiren Board WB7, OpenWrt, standard Debian, container-only, etc.>
- **Partitions and survival rules:**
  - `<mount point>` — <what lives here, does it survive firmware flash / factory reset / OS upgrade? yes / no / partial>
  - `<mount point>` — ...
- **User-editable service configs must go to:** `<path prefix>` — survives: yes / no
- **Read-only rootfs paths (wiped on flash):** `<path prefix>` — never install user data here
- **Persistent data partition:** `<path prefix>` — safe for configs, databases, user files
- **Source:** <link to platform vendor docs on filesystem layout, or "internal knowledge — owner: <name>">
- **Last reviewed:** <YYYY-MM-DD>

---

## Components

For each component of the stack (language, runtime, framework, library, target platform, integration point), one section:

### <Component name>

- **Role in this project:** <one sentence — what the component is used for here>
- **Canonical docs:** <URL — the source the maintainers point users at>
- **Spec / reference:** <URL with anchor when possible — the part of the docs the project relies on>
- **Required validators:**
  - `<command>` — <when to run, what it gates>
- **Idioms and constraints** (each item: rule + source URL):
  - <rule one>. Source: <URL>
  - <rule two>. Source: <URL>
- **Known gotchas** (issue-tracker observations, real-world traps):
  - <gotcha>. Source: <URL>
- **Last reviewed:** <YYYY-MM-DD>

Repeat per component. Keep entries focused — the goal is "what a coder must respect", not a textbook.

---

## Validators wired into pipeline

Every validator listed here must be in the project's `Pipeline` block in `CLAUDE.md`. Pipeline runs them as mandatory gates — green pipeline means every listed validator passed.

| Validator | Command | Gates |
|---|---|---|
| <name> | `<command>` | <which guarantee or constraint it enforces> |

---

## Integration contracts

For each external system the project integrates with — what local artifact carries the contract, how it gets delivered to the external system, and which tool validates the contract end-to-end.

| External system | Local artifact in repo | Delivery mechanism | Validator |
|---|---|---|---|
| <name of the external system> | `<path/in/repo/to/artifact>` | `<COPY in Dockerfile / Debian postinst / Helm chart / volume mount / CRD apply / ...>` | `<command that proves the artifact is accepted by the external system>` |

Missing rows here for a system the project actually talks to is a defect — `pm-plan-checker` blocks on missing integration contracts in the plan's Stack expectations section.

---

## How to extend this document

Only `pm-stack-researcher` edits this file. Other agents read it. If `pm-coder` or `pm-plan-checker` notices a missing rule or stale entry, they surface it to the orchestrator — orchestrator spawns `pm-stack-researcher` to update.

Each rule must cite a source URL. Unsourced claims do not belong here — they are guesses dressed as docs.
