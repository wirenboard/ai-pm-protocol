# Topology (deprecated as standalone — moved to tech-stack.md § 2)

**v0.3.0 redirect.** Topology больше не отдельный артефакт. Содержимое теперь живёт как **§ 2 в `tech-stack.md`** (Stage D, same axis — technical foundation). Stage C **упразднён** (раньше содержал только topology + reactive ADRs).

## Migration

1. Скопировать filled content (diagram / services / boundaries / data flows / non-goals) → `tech-stack.md` § 2 (subsections 2.1-2.5)
2. Удалить этот файл из `doc/`
3. Update cross-references: feature spec'и с `topology_impact: yes` → ссылаться на `tech-stack.md#topology`

`template-sync-doc-migrate.py` автоматизирует это для template-native проектов.

См. `meta/design/2026-05-25_merged-optimization-plan.md` § v0.3.0 Path C.
