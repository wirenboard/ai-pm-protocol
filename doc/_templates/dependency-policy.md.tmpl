---
skip_eligibility:
  default: optional
  skip_if: "single-developer / weekend project"
---

# Dependency Update Policy (deprecated as standalone — moved to maintenance-playbook.md Part A)

**v0.3.0 redirect.** Dependency policy больше не отдельный артефакт. Содержимое теперь живёт как **Part A в `maintenance-playbook.md`** (same axis — maintainability).

## Migration

1. Скопировать filled content в `maintenance-playbook.md` Part A (sections A.1-A.10)
2. Удалить этот файл из `doc/`

`template-sync-doc-migrate.py` автоматизирует.

См. `meta/design/2026-05-25_merged-optimization-plan.md` § v0.3.0 Path C.
