# <Project Name>

## Project

**What:** <one sentence — what the product does>

**Who:** <who uses it — be specific: "local network admin", "solo developer", "small team PM">

**Problem solved:** <what problem this replaces or eliminates for the user>

## Project kind: software

<!-- `software` (the deliverable is source code, the default) or `documentation` (the
     deliverable is one or several human-facing documents — SOP / runbook / guide / spec /
     diagrams — with no code; they live under `deliverable/` — see `### Project kind` in
     `workflow/project-kind.md`). Set by `/pm-bootstrap` from the project-kind question. An
     absent or unrecognized value is treated as `software`. -->

**Language canon:** Conversation language: the user's. Artifacts (files, code, commits, agent-authored docs): English.

---

## Architecture

### Tech stack

| Layer | Choice | Why |
|---|---|---|
| Language | <...> | <reason> |
| Framework | <...> | <reason> |
| Database | <...> | <reason> |
| <other> | <...> | <reason> |

### Architectural constraints
Agents must not violate these without an explicit PM decision:

- <e.g., "Only Postgres — chosen for horizontal scalability, no additional databases">
- <e.g., "No external API calls from frontend — security boundary">
- <add constraints specific to this project>

### Security constraints

- <e.g., "Passwords: bcrypt only, never stored plain or logged">
- <e.g., "API keys and tokens: env vars only, never in code, logs, or response headers">
- <e.g., "User input: always validated before use — no raw strings to DB or shell">

### Code conventions
AI-specific minimums (adjust in linter config if needed for this project):

- Max file length: 300 lines
- Max function/method length: 50 lines
- Cyclomatic complexity: max 10
- No file-level lint suppressions
- Test coverage: min 80% for new code

Stack linter:
```
<lint command — e.g., "npm run lint" / "golangci-lint run" / "pylint src/">
```

Comment restraint (single-sourced by `### Comment-restraint` in `.ai-pm/tooling/workflow/doc-style.md` — this section realizes that protocol rule for this project, it does not redefine it):
- Comment WHY-when-non-obvious only. Do NOT comment WHAT — well-named code says that.
- Rationale lives in the plan / arch / contract / test, not inline.
- No inline rule-ID citations (`# SC1`, `# AC6`, etc.) — rule IDs belong in the plan/test; inline citations drift.
- No docstrings on trivial or `_private` functions.
- Do NOT wire docstring-presence linters (pydocstyle / ruff `D` / pylint missing-docstring / interrogate / darglint) — they enforce presence, which is the opposite direction of comment restraint and amplifies over-documentation.

---

## Pipeline

Every command in this block must be green before coder is done. No exceptions.

**Tests + lint:**
```
<test command — e.g., "npm test" / "go test ./..." / "pytest">
<lint command — e.g., "npm run lint" / "golangci-lint run" / "pylint src/">
```

The `<lint command>` must **enforce the AI-specific minimums** (`docs/architecture.md` `### AI-specific minimums`) per the `docs/stack-notes.md` AI-minimums→linter-rule mapping — not just run a default lint. A diff that crosses a minimum must fail this step.

**Validators** (populated by `stack-researcher` at bootstrap and extended on every feature that introduces a new validator — see `docs/stack-notes.md` "Validators wired into pipeline" table):
```
<config / schema validator — e.g., "ajv validate -s schemas/foo.schema.json -d examples/*.json">
<integration dry-run — e.g., "kubectl apply --dry-run=client -f manifests/", "terraform validate">
<spec-compliance check — e.g., type-check in strict mode, framework-provided lint, native init-time validation surfaced as exit-non-zero>
```

A green tests + lint with a failing validator is still a failed pipeline. Reviewer dim 9 blocks if a validator listed in `stack-notes.md` is not present here, or if it is present here but not actually run by the coder.

---

@.ai-pm/tooling/WORKFLOW.md

---

## Docs

| File | Purpose |
|---|---|
| `docs/product.md` | **Product front door** — authored PM funnel (Why this exists / What it does today / Documents / Features), owned by `pm-architect`, PM-validated. Not generated |
| `docs/product-map.md` | **Capability map** — contract-centric (group → contract → features + reviews), PM-facing, auto-generated from contracts/plans/reviews |
| `docs/architecture.md` | Stack, decisions, constraints |
| `docs/user-journeys.md` | Existing user scenarios |
| `docs/stack-notes.md` | Stack idioms, constraints, validators, integration contracts (maintained by `pm-stack-researcher`; read by `pm-plan`, `pm-architect`, `pm-coder`, `pm-plan-checker`) |
| `docs/features/` | Past feature plans |
| `docs/ui-guide.md` | UI conventions (optional, only if project has UI) |
| `docs/threat-model.md` | Security model (optional, only if complex security needed) |
| `deliverable/` | The produced document(s) / diagrams / images — the deliverable (only on a `documentation`-kind project; open form, no mandated scaffold; see `## Project kind`) |
