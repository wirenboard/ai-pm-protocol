<!-- Doc-style rules apply: fact-first, current-state-only, provenance-as-pointer, plain-language. Read them in `## Durable-text frugality` in `workflow/doc-style.md` (`.ai-pm/tooling/workflow/doc-style.md` once installed) — the authority; this comment names the rules and points there, it does not restate them. -->

# <Product> — what it is and why

Authored product front door. Owned by `pm-architect`, validated one-pass by the PM. This is **not** the generated map — the contract→features map lives in `docs/product-map.md` (linked from `## Features` below). This file is written **without** any generated-map signature line.

## Why this exists

<In product terms: what problem the product solves and for whom. Source — the PM's answers to the product Q&A at bootstrap (why / for whom). Not technical language.>

## What it does today

<What the system does in the current version — coverage and boundaries, **including what is not yet supported** (e.g. "only dimmable light so far, no RGB"). Derived from the contracts (`.ai-pm/contracts/`, their `## User value`) and the components in `docs/architecture.md`. Refreshed by pm-architect when coverage changes (a new device/entity type, a moved boundary).>

- <does …>
- <does NOT yet …>

## Documents

<Navigation over `docs/` in the PM's language — where to look for what.>

- [Architecture](architecture.md) — stack, decisions, constraints
- [User journeys](user-journeys.md) — how the product is used
- <other docs as they appear>

## Features

Contract → features map (what each guarantee includes, which features built it, reviews): [`docs/product-map.md`](product-map.md).
