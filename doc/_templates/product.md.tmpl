# <Продукт> — что это и зачем

Authored product front door. Owned by `pm-architect`, validated one-pass by the PM. This is **not** the generated map — the contract→features map lives in `docs/product-map.md` (linked from `## Функции` below). This file is written **without** any generated-map signature line.

## Зачем это нужно

<В продуктовых терминах: какую проблему решает продукт и для кого. Источник — ответы PM на product Q&A при bootstrap (why / for whom). Не технический язык.>

## Что умеет сегодня

<Что система делает в текущей версии — покрытие и границы, **включая то, что пока не поддерживается** (например, "пока только диммируемый свет, RGB нет"). Выводится из контрактов (`.ai-pm/contracts/`, их `## User value`) и компонентов из `docs/architecture.md`. Обновляется pm-architect при изменении покрытия (новый тип устройства/сущности, сдвиг границы).>

- <умеет …>
- <пока НЕ умеет …>

## Документы

<Навигация по `docs/` на языке PM — куда смотреть за чем.>

- [Архитектура](architecture.md) — стек, решения, ограничения
- [Сценарии пользователя](user-journeys.md) — как продуктом пользуются
- <другие docs по мере появления>

## Функции

Карта «контракт → фичи» (что каждая гарантия включает, какие фичи её строили, ревью): [`docs/product-map.md`](product-map.md).
