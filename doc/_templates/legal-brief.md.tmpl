---
skip_eligibility:
  default: conditional
  skip_if: "processes-pii = no AND no payments AND no GDPR-jurisdiction"
  hard_floor: "never if processes-pii = yes"
---

# Legal Brief (deprecated as standalone — moved to legal.md § 2)

**v0.3.0 redirect.** Legal brief больше не отдельный артефакт. Объединено с `legal-frame` (раньше предполагавшимся отдельным документом) в единый **`legal.md`** — same axis quality (legal / compliance).

## Migration

1. Скопировать filled content (ToS / PP / Cookie / Open questions / Budget) в `legal.md` § 2 (sections 2.1-2.5)
2. Удалить этот файл из `doc/`

`template-sync-doc-migrate.py` автоматизирует.

См. `meta/design/2026-05-25_merged-optimization-plan.md` § v0.3.0 Path C.
